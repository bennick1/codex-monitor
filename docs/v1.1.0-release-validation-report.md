# Codex Monitor v1.1.0 — Release Validation Report

**Final state: Awaiting Human Acceptance**

Local release-preparation gates passed after the explicitly authorized Vitest security update. Actions candidate builds and final package human acceptance are pending. No Tag, GitHub Release, Draft Release or Pre-release is created or authorized by this task.

## Provenance

| Item | Evidence / status |
| --- | --- |
| Release Preparation start SHA | c48fedea6893811bd6fccc4dceb6b17fab5a95e9 |
| Security dependency commit | f162ac0 — chore: update Vitest for security advisory |
| Release Build Source SHA | Pending release infrastructure commit and verified fast-forward push |
| Latest Documentation SHA | Resolve `git log -1 --format=%H -- docs/v1.1.0-release-validation-report.md`; later docs-only evidence commits do not change binary provenance |
| Workflow Run ID / URL | Pending |
| Workflow head_sha / conclusion | Pending |
| Source scope | Authorized Vitest security dependency update, release workflow, artifact script, release documentation and necessary README only |

Recovery git fetch succeeded and origin/main matched the start SHA. The release worktree was clean before preparation. Original workspace uncommitted tests and private runtime report are untouched. No production code, existing tests, SQLite schema, model attribution, repair, Quota Week or Token counting rules changed.

Future v1.1.0 Tag must point to Release Build Source SHA, never a later docs-only evidence commit. A document cannot embed its own commit SHA; the history command above identifies its exact revision.

## Security dependency remediation

The original full audit had two moderate package entries for the same [GHSA-82fw-gwwq-j7x9](https://github.com/advisories/GHSA-82fw-gwwq-j7x9) advisory, vitest / @vitest/mocker 3.2.7. The user explicitly authorized Vitest 4.1.11 within release preparation.

- package.json changes only Vitest from `^3.2.4` to `^4.1.11`.
- package-lock.json resolves vitest and all seven installed @vitest packages to **4.1.11**. Diff: 130 added / 208 removed lines, including normal Vitest test-only transitive changes (chai, es-module-lexer, std-env, tinyexec, tinyrainbow, obug and @standard-schema/spec) and removal of obsolete Vitest 3 dependencies.
- All existing production lock entries are byte-for-byte equivalent as parsed objects. Vite 7.3.6, jsdom 26.1.0, Testing Library, React and Tauri entries remain unchanged. No overrides, audit fix, force fix or threshold relaxation.
- npm 10.9.8 installation encountered an Arborist `edgesOut` exception before changing either package file. A one-time temporary npm 11 invocation completed the same targeted install with prefer-dedupe; no global npm or project toolchain configuration changed. Normal npm 10.9.8 npm ci and all subsequent npm gates passed against the resulting lockfile.
- Existing tests passed directly after upgrade: **97/97 in 12 files**. No test edits. The original workspace's two uncommitted tests are intentionally excluded; no claim of 99 committed tests.

## Automated validation

| Gate | Local | macOS CI | Windows CI |
| --- | --- | --- | --- |
| npm ci | Passed, Node 22.23.1 / npm 10.9.8 | Pending | Pending |
| Frontend tests | 97 passed / 12 files, Vitest 4.1.11 | Pending | Pending |
| npm run build | TypeScript + Vite passed | Pending | Pending |
| Full npm audit --audit-level=high | 0 vulnerabilities | Pending | Pending |
| npm audit --omit=dev --audit-level=high | 0 vulnerabilities | Pending | Pending |
| Updater policy | Passed | Pending | Pending |
| Rust fmt | Passed | Pending | Pending |
| Rust tests --locked | 67 passed / 2 ignored / 0 failed | Pending | Pending |
| Rust clippy --all-targets --locked -- -D warnings | Passed | Pending | Pending |
| git diff --check / scope | Passed; explicit file staging | N/A | N/A |
| Artifact script synthetic checks | Nine scenarios passed | N/A | N/A |

Two ignored Rust entries are the crash-test child helper and explicit 1 GiB performance test. They are not counted as passed. The feature report holds prior explicit performance evidence; no new performance rerun is claimed.

Artifact script checks cover both platforms, old-only rejection, version-lookalike rejection, duplicate rejection, different-digest overwrite rejection, same-digest repeat, empty rejection, old/current isolation and non-NSIS rejection. Synthetic fixtures are not candidates.

## Candidate packages

| Item | macOS | Windows |
| --- | --- | --- |
| File | Codex-Monitor-1.1.0.dmg | Codex-Monitor-1.1.0.exe |
| Architecture | Universal arm64 + x86_64 verification pending | x64 NSIS verification pending |
| Version / identifier | 1.1.0 / app.quotafloat.desktop; package verification pending | 1.1.0 / app.quotafloat.desktop; build verification pending |
| SHA256 | Pending | Pending |
| Actions internal SHA vs local SHA | Pending | Pending |
| Package validation | hdiutil verify, mounted metadata, file/lipo pending | CI normal-user silent install, registration, launch for 8 seconds, stop, silent uninstall pending |
| Signing / notarization | Unsigned / Not Notarized; actual signature inspection pending; ad-hoc is not Developer ID | Unsigned publisher |
| Human acceptance | Pending Actions DMG final package smoke | Pending real-machine acceptance |

## Upgrade, model attribution and Quota Week

The retained committed Rust suite passed, including `v1_migration_and_metadata_backfill_preserve_all_accounting_and_deleted_sources`, `failed_v1_migration_rolls_back_and_foreign_v1_database_is_untouched` and `late_model_metadata_initialization_failure_rolls_back_all_ddl`. These preserve schema 1 → 2 upgrade, accounting/Token Fact and four Overview total equality, metadata backfill, restart, incremental update and complete failure rollback.

The [feature validation report](v1.1.0-feature-validation-report.md) separately records original V1 native synthetic upgrade and downgrade-refusal evidence. That evidence is carried forward, not described as rerun Actions package testing.

Model evidence still comes from local rollout metadata. Unreliable attribution remains Unidentified; no 100% attribution promise. Overview remains today / thisWeek / thisMonth / total. By Model remains today / quotaWeek / thisMonth / total. Quota Week uses the current valid weekly quota window, `weeklyResetAt - 604800 seconds` to `queryAt`, not a natural-week fallback.

## Human acceptance

Prior complete macOS feature/window acceptance is recorded in the feature report. It does not establish acceptance of the final Actions DMG. Final package smoke must cover installation, launch, Quota, Overview, By Model, default Quota Week, historical model data, Hover, and Full-screen Space.

Windows CI is not real-machine acceptance. Use the [Windows human checklist](v1.1.0-windows-human-acceptance-checklist.md); the user supplies pass/fail. No detailed machine identifiers or real usage belong in this report.

## Known limitations and release state

- macOS unsigned / not notarized; no Apple verification or Developer ID claim. Windows unsigned publisher.
- Token Statistics are local, not official account-wide or cross-device totals; historical attribution may be Unidentified.
- v1.1.0 uses SQLite schema 2. v1.0.0 cannot read it and its Token Statistics become unavailable. The previously verified refusal path does not damage the database; v1.1.0 can reopen it. Downgrade compatibility is not supported.
- Updates remain manual through bennick1/codex-monitor/releases; no Tauri updater runtime or automatic download/install.
- No automatic quota-to-Token capacity prediction.
- Initial backfill/restart integrity checks can take time; missing history can be retried later.

**Awaiting Human Acceptance.** Local checks are complete; Actions run/candidate fields above remain pending. Release Notes approval, both final candidate human acceptances and explicit user “可以发布” remain outstanding.
