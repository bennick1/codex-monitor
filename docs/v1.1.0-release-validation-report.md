# Codex Monitor v1.1.0 — Release Validation Report

**Final state: Released — v1.1.0**

The final production/UI SHA is `9281644b48f93a1ef4a4bda99b1e8f8368dd6f2d`. The Windows interaction root cause is confirmed, the mousedown-isolation fix is implemented, and **Windows Fix Human Validation = Passed**, as reported by the user on a real Windows machine. The final UI adjustment is complete. Previous candidates are **Superseded**. The docs-only `release: prepare final v1.1.0 rebuild` commit locked **FINAL_RELEASE_BUILD_SOURCE_SHA = `0eee3588197ef42298ab75bf945f62a226159ff3`** after all local quality gates passed. Release Preparation Run #34571067271 then succeeded for both platforms from that exact source.

Historical Candidate A, source `36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4` / Run #34306184431, was superseded by the approved model-period adjustment. Historical Candidate B, source `0e7ca35da7a9b417caa685cc2fe615011cd24a73` / Run #34454590766, is **Superseded by Windows interaction fix and final UI adjustment**. Their packages, hashes, job/artifact IDs and historical observations are retained below; none may be reused or mixed with the final rebuild.

The current product contract is Overview `Today / This Week / This Month / Total`; By Model `Today / Quota Period / 7 Days / 30 Days / Total`, defaulting to Quota Period; Chinese `今日 / 额度周期 / 近7天 / 近30天 / 总计`; and the free built-in skins Default, Blur, and Computer. The preparation stage did not authorize publication; the subsequent explicit user release authorization and final-candidate acceptance are recorded in Release Closure below.

The final Expanded Card title is **Codex-Monitor**. The Token footer no longer displays the overall incomplete-statistics status, local collected-usage text, or successful-collection time, in either language. Value-level partial `*`, aria labels and tooltips remain. The user's preceding UI validation reported 127 frontend tests and 541 synthetic cases with zero failures; this rebuild's fresh local and Actions quality gates are recorded below.

The published annotated v1.1.0 Tag targets **FINAL_RELEASE_BUILD_SOURCE_SHA**, `0eee3588197ef42298ab75bf945f62a226159ff3`, the actual source of the final two-platform Actions run. It does not target the production/UI SHA, an old candidate, **FINAL_RELEASE_EVIDENCE_DOCS_SHA**, or the later closure documentation commit. Both final-candidate human acceptance results are **Passed**, supplied by the user together with explicit release authorization.

## Historical Candidate A — 36bcd2c / Run #34306184431 — Superseded

Run #34306184431, Codex-Monitor-1.1.0.dmg, Codex-Monitor-1.1.0.exe and SHA256SUMS: **Superseded by approved model-period adjustment**. Preserve all evidence below for historical traceability only. Do not use these packages for Windows human acceptance, official checksums or v1.1.0 Release, and do not mix them with subsequent artifacts.

The source 36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4 is a **Superseded candidate source** and must not be the final v1.1.0 Tag target. After feature acceptance, stop at **Feature Adjustment Ready for Release Preparation** and await explicit approval for a new Release Preparation / Closure, source SHA, two-platform workflow, DMG / EXE / SHA256SUMS and final human acceptance. No new release build source is designated here.

### Historical preparation evidence (superseded)

The complete macOS/Windows Release Preparation run succeeded. Both final candidate installers come from the same verified source SHA. Checksums, local Actions DMG inspection and partial tool-assisted native package smoke are complete. Windows real-machine acceptance and macOS final human acceptance remain pending. No Tag, GitHub Release, Draft Release or Pre-release was created.

### Historical build source and documentation provenance

| Item | Evidence |
| --- | --- |
| Original preparation baseline | c48fedea6893811bd6fccc4dceb6b17fab5a95e9 |
| Vitest security commit | f162ac094ec682588add61e07fecef79ab4fbd4e |
| Initial release infrastructure commit | c90926f2c4f6da752e6d3758bdc1abb09c2b7875 |
| Workflow fix commit | **36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4** — fix: correct macOS universal architecture check |
| Superseded candidate source | **36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4** |
| origin/main when dispatched | 36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4; ordinary fast-forward push verified |
| Latest Documentation SHA | The subsequent docs-only commit containing this completed report; resolve exactly with `git log -1 --format=%H -- docs/v1.1.0-release-validation-report.md` |
| Successful workflow | [Release Preparation #34306184431](https://github.com/bennick1/codex-monitor/actions/runs/34306184431) |
| Run head_sha / conclusion | 36bcd2cb570c7cb1ad48d4add3fb4347ff9ed0e4 / **success** |
| macOS job | [102323253634](https://github.com/bennick1/codex-monitor/actions/runs/34306184431/job/102323253634) — success |
| Windows job | [102323254536](https://github.com/bennick1/codex-monitor/actions/runs/34306184431/job/102323254536) — success |

The workflow fix contains exactly one line change in `.github/workflows/release.yml`. The then-uncommitted failed-run evidence report was deliberately excluded. This later documentation update changes neither the binaries nor their source. A document cannot embed its own commit hash; use the command above or the report's Git history to identify the documentation revision. **Historical target guidance is superseded: neither this former candidate source nor its later docs-only commit is the final v1.1.0 Tag target.**

No production code, test code, SQLite schema, model attribution, historical repair, Quota Week or Token accounting changes were made during preparation. The only dependency change was the explicitly approved Vitest security update. Original workspace uncommitted tests and sensitive runtime report were preserved.

### Historical previous failure and minimal correction

[Previous failed Run #34303934216](https://github.com/bennick1/codex-monitor/actions/runs/34303934216) used c90926f2c4f6da752e6d3758bdc1abb09c2b7875. Windows passed. macOS built a Universal binary and passed bundle metadata, but the newly added lipo command put its input after the architecture list. lipo interpreted the path as an architecture flag and exited 1.

The single-line correction is:

```sh
lipo "$app_path/Contents/MacOS/$executable" -verify_arch arm64 x86_64
```

The full two-platform matrix was dispatched anew after committing and pushing the correction. **No Windows artifact from #34303934216 was reused.** Both artifact records from the successful run carry workflow_run.head_sha equal to the final Release Build Source SHA.

### Historical security dependency remediation and automated gates

Vitest and all seven installed @vitest packages, including @vitest/mocker, resolve to **4.1.11**. This resolved the sole [GHSA-82fw-gwwq-j7x9](https://github.com/advisories/GHSA-82fw-gwwq-j7x9) advisory. package.json changes only Vitest (`^3.2.4` → `^4.1.11`). package-lock.json changes the Vitest test dependency chain (130 additions / 208 deletions), including normal new/removed transitives. No unrelated production lock entry changed; Vite, jsdom, Testing Library, React and Tauri remained unchanged.

npm 10.9.8's targeted install first hit an Arborist edgesOut exception without file changes. A temporary npm 11 targeted install with prefer-dedupe succeeded; no global npm or project toolchain change. Subsequent normal npm ci and all quality gates passed. No force fix, override, test rewrite or reduced audit threshold was used.

| Gate | Local after security update | New macOS CI | New Windows CI |
| --- | --- | --- | --- |
| npm ci | Passed | Passed | Passed |
| Frontend tests, Vitest 4.1.11 | **97 passed / 12 files**, unchanged tests | Passed | Passed |
| TypeScript + Vite build | Passed | Passed | Passed |
| Full npm audit --audit-level=high | **0 vulnerabilities** | **0 vulnerabilities** | **0 vulnerabilities** |
| npm audit --omit=dev --audit-level=high | **0 vulnerabilities** | **0 vulnerabilities** | **0 vulnerabilities** |
| Updater policy | Passed | Passed | Passed |
| Cargo fmt --all -- --check | Passed | Passed | Passed |
| Cargo test --locked | 67 passed / 2 ignored | 67 passed / 2 ignored | 63 passed / 2 ignored |
| Cargo clippy --all-targets --locked -- -D warnings | Passed | Passed | Passed |
| Candidate build | Not substituted for Actions builds | Passed, app + Universal DMG | Passed, x64 NSIS |
| Bundle version/name/identifier | Checked on downloaded DMG | Passed | Version/config check passed |
| file + lipo | Two architectures; lipo exit 0 | Passed | N/A |
| Windows install/start/uninstall smoke | N/A | N/A | Passed |
| Artifact preparation/upload | Downloaded new run only | Passed | Passed |
| git diff --check / scope | Passed, explicit file staging | N/A | N/A |

Two ignored Rust entries are the crash-test child helper and explicit 1 GiB performance test, not counted as passed. Prior explicit performance evidence remains in the feature report. The original workspace's two uncommitted frontend cases were excluded; this does not claim 99 committed tests.

The artifact script's nine prior synthetic checks passed: both platforms, old-only rejection, version-lookalike rejection, duplicate rejection, different-digest overwrite rejection, same-digest repeat, empty rejection, old/current isolation and non-NSIS rejection. It was not changed for the lipo fix.

### Historical candidates and checksums

| File | Platform | Bytes | Actions artifact ID |
| --- | --- | --- | --- |
| Codex-Monitor-1.1.0.dmg | macOS Universal | 11,486,254 | 10086822352 |
| Codex-Monitor-1.1.0.exe | Windows x64 NSIS | 3,997,977 | 10086820302 |

Both artifacts' build-environment.json commit and run URL match the final source and successful run. Each downloaded installer SHA matches its artifact's internal SHA256SUMS. The merged local manifest was checked with `shasum -a 256 -c SHA256SUMS`: **both OK**. These are installer hashes, not the outer Actions ZIP digests.

```text
86794029e0753a7b21bc0a0c0ed6b334a7701b8328dfa83ad6bd15d5478e6f29  Codex-Monitor-1.1.0.dmg
02734a88c9a6ff95441fb99a83d8f1a59e2223a17a12343cf2504d9d9270fc87  Codex-Monitor-1.1.0.exe
```

Local deliverables are under ignored `outputs/v1.1.0/`: the two installers, SHA256SUMS, and an evidence subdirectory with sanitized build/run/artifact/package-check records. No installers, credentials, SQLite files, personal screenshots or real Token/Quota values are committed.

### Historical macOS Actions DMG validation and package smoke

- hdiutil verify: **passed**. Mounted the downloaded image read-only; detached it after verification and installation.
- Bundle: **Codex Monitor.app**, CFBundleDisplayName **Codex Monitor**, CFBundleShortVersionString **1.1.0**, CFBundleIdentifier **app.quotafloat.desktop**.
- `file` reported **x86_64 and arm64**. `lipo <actual binary> -verify_arch arm64 x86_64` returned **0**, both in CI and locally. **macOS Universal architecture validation = Passed.**
- Actual codesign inspection: **ad-hoc linker signature**, TeamIdentifier not set, no CMS Developer ID signature. Distribution remains **Unsigned / Not Notarized**; no Apple verification or Gatekeeper-first-download approval is claimed.
- Installed the exact Actions app into `/Applications/Codex Monitor.app`, preserving the previous application bundle separately. Every installed file matched the mounted DMG. No database/settings cleanup or replacement was performed.
- The running process path was verified as `/Applications/Codex Monitor.app/Contents/MacOS/codex-monitor`, version 1.1.0, native WebView `tauri://localhost`. Executable SHA256: `810874074b3014592bc36d2afd0aee14f65aac5871cb5cb00eb7a9014613ab53`, equal to the DMG binary. This was not the old debug or feature candidate.

| Final package smoke item | Actual observation |
| --- | --- |
| Installation / launch | Passed through tool-assisted native operation |
| Quota | Available quota observed; a transient response-format message also appeared and subsequently recovered to synchronized quota. No real values retained; user should confirm stability during normal use |
| Overview | Existing local totals and partial-state display observed |
| By Model | Model rows and shares observed |
| Default Quota Week | First By Model entry selected Quota Week |
| Historical models | Total period contained historical model rows and Unidentified |
| Expand / collapse | Expanded/collapsed states observed through native tool interaction |
| Pure Hover behavior | **Pending human confirmation**; click-assisted observations are not labeled pure-hover proof |
| Full-screen Space | A temporary Finder window was actually moved into and out of native fullscreen; the app-isolated screenshot could not establish the cross-app overlay position. **Pending human confirmation**, not labeled pass/fail |

Temporary fixed-expansion setting was restored off, existing always-on-top was retained, Overview restored, and the temporary Finder window closed. No personal screenshot or real usage was written to the report/evidence. This is partial tool-assisted **final package smoke**, separate from the earlier complete macOS feature acceptance. **macOS Human Acceptance = Pending.**

### Historical Windows Actions candidate

The new run's Windows job passed normal-user NSIS silent installation, uninstall registration lookup, installed codex-monitor.exe discovery, launch, **8-second survival**, process termination, and silent uninstall. It built and uploaded the EXE from the same final source as the DMG.

**Windows Human Acceptance = Pending.** GitHub's runner is not real-machine acceptance. Use the [Windows human checklist](v1.1.0-windows-human-acceptance-checklist.md) with this new EXE; the user/colleague provides pass/fail. Do not reuse the old failed run's EXE or publish detailed machine/usage records.

### Historical upgrade and preserved product boundaries

The unchanged Rust migration suite passed again in the new matrix. It covers schema 1 → 2, unchanged accounting/Token Facts and Overview totals, model metadata backfill, restart, incremental update, early and late migration failure rollback. Prior independent native V1 upgrade and downgrade-refusal evidence remains in the [feature report](v1.1.0-feature-validation-report.md); no claim that those full original harnesses were rerun on a Windows real machine.

Overview remains today / thisWeek / thisMonth / total. By Model now uses today / quotaPeriod / last7Days / last30Days / total and defaults to quotaPeriod. Quota Period uses the valid actual seven-day quota window, weeklyResetAt − 604800 seconds to queryAt, with no natural-week fallback. The 7 Days and 30 Days entries are exact rolling durations, not the natural week or current month. Attribution still depends on reliable local rollout model metadata; unknown history remains Unidentified. Manual update entry remains bennick1/codex-monitor/releases, with no Tauri updater runtime or automatic download/install.

### Historical known limitations and final state

- macOS unsigned / not notarized; Windows unsigned publisher.
- Local Token Statistics are not official account-wide or cross-device totals; historical attribution may remain Unidentified.
- v1.1.0 uses SQLite schema 2. v1.0.0 cannot read it and its Token Statistics become unavailable. The previously verified refusal path preserves the database, and v1.1.0 can reopen it; downgrade compatibility is not supported.
- Initial backfill/restart integrity checks may take time; missing sources may be retried.
- Updates are manual; no automatic quota-to-Token capacity prediction.
- Pure Hover, final fullscreen appearance and stable quota behavior require final macOS human confirmation; Windows real-machine acceptance is outstanding.

**Superseded — model period adjustment approved.** Historical candidate readiness is not release authorization. Both human acceptance results, Release Notes approval and explicit user **“可以发布”** remain required. No Tag or any GitHub Release was created.

## Historical Candidate B — 0e7ca35 / Run #34454590766 — Superseded

**Superseded by Windows interaction fix and final UI adjustment.** The prior release evidence docs SHA is `2c73ad9affe428dfa88d73697360814db68459be`. Subsequent user-supplied acceptance: **old macOS Human Acceptance = Passed**; **old Windows Human Acceptance = Blocked**, because By Model clicks could appear unresponsive or delayed. The later **Windows Fix Human Validation = Passed** confirms the fix's effectiveness; it does not reinstate either installer. The originally recorded package checks and pending results below are preserved as historical observations from that run.

| Item | Historical Candidate B evidence |
| --- | --- |
| Initial `main` | `c1a237792822705ddda1d93b328ce543540cc8ce` |
| Production-code candidate | `e490df334fce25a17a0ed8c7312bed1c94daef9f` |
| Release preparation branch | `codex/v1.1.0-release-preparation` |
| **Release Build Source SHA** | **`0e7ca35da7a9b417caa685cc2fe615011cd24a73`** |
| Build-source commit | `release: finalize v1.1.0 release preparation baseline`; five current-state documentation files only |
| Workflow | [Release Preparation #34454590766](https://github.com/bennick1/codex-monitor/actions/runs/34454590766) |
| Run source / result | `main` / `0e7ca35da7a9b417caa685cc2fe615011cd24a73` / **success** |
| macOS Job | [102797928234](https://github.com/bennick1/codex-monitor/actions/runs/34454590766/job/102797928234) — **success** |
| Windows Job | [102797928550](https://github.com/bennick1/codex-monitor/actions/runs/34454590766/job/102797928550) — **success** |
| **Release Evidence Docs SHA** | `2c73ad9affe428dfa88d73697360814db68459be`; historical documentation, not a Tag target |

The workflow was manually dispatched from `main` only after the ordinary fast-forward of the build-source commit. Both matrix jobs completed every applicable policy, version, test, build, audit, Rust quality, packaging and artifact-upload step. The Windows package-smoke step passed; the macOS bundle/Universal verification step passed. A GitHub infrastructure annotation noted that selected official actions targeting Node.js 20 were run by the hosted runner on Node.js 24; it did not fail or weaken a gate.

### Historical Candidate B files and provenance

| Platform | File | Bytes | Installer SHA256 | Actions Artifact ID |
| --- | --- | ---: | --- | ---: |
| macOS Universal | `Codex-Monitor-1.1.0.dmg` | 12,674,043 | `95bb509f408835aa38e5985ebcdcbf59fa78f07422697f8e04ee62a5ebe557f0` | 10143051404 |
| Windows x64 NSIS | `Codex-Monitor-1.1.0.exe` | 4,591,276 | `6a49f11b93b0162b23123eb5aea0f078980f1359d8b0e2473c3224da70208106` | 10143083830 |

- Artifact `codex-monitor-v1.1.0-macos-universal-unsigned` and artifact `codex-monitor-v1.1.0-windows-x64-unsigned` both belong to Run #34454590766 and carry `workflow_run.head_sha` equal to the Release Build Source SHA.
- Both artifacts' `build-environment.json` record commit `0e7ca35da7a9b417caa685cc2fe615011cd24a73` and the Run #34454590766 URL.
- Each installer passed verification against its artifact's internal `SHA256SUMS`. A new combined manifest containing only the new DMG and EXE also passed `shasum -a 256 -c SHA256SUMS` for both files.
- The two installer hashes differ from the superseded hashes. Only artifacts from the new run were downloaded into a new ignored output directory; no old candidate file, manifest or evidence was overwritten or mixed in.

### Historical Candidate B local and CI quality gates

| Gate | Local build-source result | macOS CI | Windows CI |
| --- | --- | --- | --- |
| npm ci | Passed with an isolated cache after the user cache proved unwritable; no project or dependency change | Passed | Passed |
| Updater policy | Passed; manual GitHub Release download only | Passed | Passed |
| Frontend tests | **103 passed / 12 files** | Passed | Passed |
| TypeScript + Vite build | Passed | Passed | Passed |
| Full npm audit, high threshold | **0 vulnerabilities** | **0 vulnerabilities** | **0 vulnerabilities** |
| Runtime-only npm audit, high threshold | **0 vulnerabilities** | **0 vulnerabilities** | **0 vulnerabilities** |
| Cargo fmt `--all -- --check` | Passed | Passed | Passed |
| Cargo test `--locked` | **73 passed / 2 ignored / 0 failed** | Passed | Passed |
| Cargo clippy `--all-targets --locked -- -D warnings` | Passed | Passed | Passed |
| Candidate build / packaging | Not substituted for Actions outputs | Universal app + DMG passed | x64 NSIS passed |
| Git scope / `git diff --check` | Passed; five approved docs in the build-source commit | N/A | N/A |

The local npm cache and Rust command-discovery issues were environment conditions, not quality-gate failures. They were resolved with isolated or already-installed temporary toolchain locations without changing code, workflow, dependencies or gate thresholds.

### Historical Candidate B macOS verification

- `hdiutil verify`: **passed**. The new DMG was then mounted read-only and detached after inspection.
- Bundle metadata: `CFBundleDisplayName = Codex Monitor`, `CFBundleShortVersionString = 1.1.0`, `CFBundleIdentifier = app.quotafloat.desktop`.
- `file`: Mach-O Universal binary containing **x86_64** and **arm64**.
- `lipo <binary> -verify_arch arm64 x86_64`: **exit 0**.
- `codesign -dv --verbose=4`: ad-hoc linker signature; `TeamIdentifier=not set`; no Developer ID identity.
- Gatekeeper/signing evidence: `spctl` rejected the app with no usable distribution signature, and `stapler` found no ticket. Status is **Unsigned / ad-hoc / Not Notarized**. No Developer ID signing, Apple notarization, downloaded-first-install or Gatekeeper acceptance is claimed.
- Package smoke used the actual app copied from this downloaded DMG, not an old candidate, local feature build or debug build. Its running executable path was checked against that isolated app copy; no existing application bundle was overwritten.
- The app launched normally, showed the Orb, expanded after pointer entry with click-assisted observation, and exposed both Overview and By Model. By Model opened on `额度周期`; `今日 / 额度周期 / 近7天 / 近30天 / 总计` were present and each was selected successfully. The obsolete `按额度周` label was not present.
- The package loaded and rendered Blur, Computer and Default in sequence, with three distinct rendering fingerprints; Default was restored during the check. The corresponding native theme-menu transition, persistence and default-restoration path also passed the unchanged Rust test in local and Actions runs. Because the tray menu itself was not exposed to the automation accessibility surface, direct human tray selection remains part of final macOS Human Acceptance rather than being overstated here.
- Before skin checks, the real preference file was copied only to a temporary local backup. After the checks it was restored byte-for-byte, all temporary preference copies were removed, and the previously running local validation app was restarted. No Token Statistics database or other application data was removed or replaced.

This is a package smoke, not final macOS Human Acceptance. Pure-hover behavior, direct tray skin selection, normal downloaded installation and the remaining real-user interaction gates are **Pending**.

### Historical Candidate B Windows verification

The successful Windows Job built the new x64 NSIS installer and passed normal-user silent installation, uninstall-registry lookup, installed `codex-monitor.exe` discovery, launch, **8-second survival**, process termination and silent uninstall. Artifact naming, SHA256 generation and upload then passed in the same job.

**Windows CI Package Smoke = Passed. Windows Human Acceptance = Pending.** A GitHub-hosted runner is not a real Windows-machine human acceptance result.

### Historical Candidate B security and release boundary

- Current-state documentation was corrected without modifying production code, tests, workflow, dependencies, package configuration, SQLite schema, model attribution, Token accounting or updater behavior.
- No installer, personal screenshot, real Token/model usage, Quota percentage, reset timestamp, real preferences, SQLite/WAL/SHM, `.codex`, `auth.json`, local output directory or machine-specific absolute path is included in the Git changes.
- The downloaded installers and their merged manifest remain ignored local validation outputs. They were not attached to a GitHub Release and were not published.
- No v1.1.0 Tag, Draft Release, Pre-release or GitHub Release was created. Both the release-preparation branch and the feature branch remain preserved.
- Historical Tag guidance for `0e7ca35da7a9b417caa685cc2fe615011cd24a73` is **superseded**. The future Tag must point to this final rebuild's actual **FINAL_RELEASE_BUILD_SOURCE_SHA**, never this old source or a later evidence-docs commit.

## Final Candidate — 0eee358 / Run #34571067271

| Item | Final rebuild evidence |
| --- | --- |
| Old `origin/main` | `2c73ad9affe428dfa88d73697360814db68459be` |
| Final production/UI SHA | `9281644b48f93a1ef4a4bda99b1e8f8368dd6f2d` |
| Feature/main relation before merge | Feature ahead 4 / behind 0; merge base exactly old `origin/main` |
| Original four-commit chain | `19a67599d33508eccd854d0adcb5b321734ecdaf` → `7faf79e5bf6c41bdf8c91989c153cd8d567e231a` → `41122f027c28405e380017525c21e2e9536f90c5` → `9281644b48f93a1ef4a4bda99b1e8f8368dd6f2d` |
| Merge result | Ordinary non-force fast-forward to `9281644`; no merge commit, squash, rebase, amend or history rewrite |
| Final rebuild branch | `codex/v1.1.0-final-rebuild`, created clean from that main |
| **FINAL_RELEASE_BUILD_SOURCE_SHA** | **`0eee3588197ef42298ab75bf945f62a226159ff3`** |
| Build-source commit | `release: prepare final v1.1.0 rebuild`; only this report and the Windows interaction-fix report |
| `origin/main` when dispatched | `0eee3588197ef42298ab75bf945f62a226159ff3`, ordinary fast-forward verified |
| **FINAL_RUN_ID** | [**34571067271**](https://github.com/bennick1/codex-monitor/actions/runs/34571067271) |
| Workflow / event / branch | **Release Preparation / workflow_dispatch / main** |
| Run `head_sha` / conclusion | **`0eee3588197ef42298ab75bf945f62a226159ff3` / success** |
| macOS Job | [103173080930](https://github.com/bennick1/codex-monitor/actions/runs/34571067271/job/103173080930) — **success** |
| Windows Job | [103173080784](https://github.com/bennick1/codex-monitor/actions/runs/34571067271/job/103173080784) — **success** |
| **FINAL_RELEASE_EVIDENCE_DOCS_SHA** | The docs-only commit `docs: record final v1.1.0 rebuilt candidates`; resolve with `git log -1 --format=%H --grep='^docs: record final v1.1.0 rebuilt candidates$'`. This is not the future Tag target |
| Future Tag target | **`0eee3588197ef42298ab75bf945f62a226159ff3`** only; not `9281644`, old candidates, later evidence docs or latest main |

### Final candidates and provenance

| Platform | Filename | Bytes | SHA256 | Artifact ID |
| --- | --- | ---: | --- | ---: |
| macOS Universal | `Codex-Monitor-1.1.0.dmg` | 12,672,890 | `008fe507223c928f0732ec640a385cfaa7c2b7b9139b59927e8ad377b9d5c9ad` | 10187882400 |
| Windows x64 NSIS | `Codex-Monitor-1.1.0.exe` | 4,590,003 | `a9920fcc8a626f5198f7b13e1dd9882beab0097c50cfa01b911065df177df0ee` | 10187912138 |

- Only `codex-monitor-v1.1.0-macos-universal-unsigned` and `codex-monitor-v1.1.0-windows-x64-unsigned` from Run #34571067271 were downloaded, into a new isolated, ignored `outputs/v1.1.0-final-rebuild/` directory. No prior installer, manifest or evidence was overwritten or reused.
- Both Actions artifact records identify the same Run and Build Source. Both `build-environment.json` files contain `commit = 0eee3588197ef42298ab75bf945f62a226159ff3` and `runUrl = https://github.com/bennick1/codex-monitor/actions/runs/34571067271`.
- Each installer passed its own artifact's internal `SHA256SUMS`. New SHA256 values were computed from the downloaded bytes, and the new combined `SHA256SUMS` passed `shasum -a 256 -c SHA256SUMS` for both files. The hashes above are installer hashes, not outer ZIP digests.
- The old Run #34454590766 hashes `95bb509f408835aa38e5985ebcdcbf59fa78f07422697f8e04ee62a5ebe557f0` and `6a49f11b93b0162b23123eb5aea0f078980f1359d8b0e2473c3224da70208106` remain **Superseded**.

### Final macOS package inspection and smoke

- **hdiutil verify: Passed.** The exact new DMG was mounted read-only and detached after inspection and isolated installation.
- Metadata: **CFBundleDisplayName = Codex Monitor**, **version = 1.1.0**, **identifier = app.quotafloat.desktop**.
- `file` and `lipo -archs` report **arm64 + x86_64**. On the same mounted executable, `lipo <binary> -verify_arch arm64` and `lipo <binary> -verify_arch x86_64` each returned **0**. The local tool rejected the combined architecture list with `-verify_arch requires exactly one input file`; this diagnostic is retained. The unchanged Actions combined `lipo <binary> -verify_arch arm64 x86_64` gate passed. Both required architectures were verified; no workflow or gate was weakened.
- `codesign -dv --verbose=4`: **Signature=adhoc; TeamIdentifier=not set**. `spctl` assessment rejected the app (exit 3); `stapler validate` found no usable ticket (exit 65). Distribution remains **ad-hoc / no Developer ID / Not Notarized**. No Gatekeeper or downloaded-first-install acceptance is claimed.
- The app copied from the new DMG to an isolated installation directory matched all three file/symlink manifest entries, including file hashes and modes. The installed executable SHA256 is `fc2b92adbc16167f91e86d4a73a51ca54e358eb84d584e7c752370433b5eab59`, matching the DMG binary.
- **Launch = Passed.** After the earlier automatic-approval rejection, the user explicitly authorized this exact final macOS app. The native tool launched the verified DMG copy. The unique running process path and executable hash matched that copy; the native WebView is `tauri://localhost`. The earlier capture error no longer blocked observation.
- Orb and expanded card rendered. Native AX and visual inspection confirmed `Codex-Monitor`, absent Token metadata footer, visible partial asterisks and retained partial accessible labels. Overview / By Model and all five periods selected successfully. Expansion was observed during tool pointer-entry/click operation; the user subsequently confirmed normal Hover on this running final app. At package-smoke time, full final-candidate human acceptance remained Pending; the later user-supplied Passed result is recorded below.
- **Default / Blur / Computer rendering = Passed on the final app.** The native tray menu was not exposed to the automation tool. With the app stopped, its settings were privately backed up and only `selectedSkin` was temporarily changed. Each skin was inspected after starting the same verified final app. The original settings bytes were restored, followed by turning the temporary stay-expanded toggle off through the native UI. Final skin is the original Blur, appearance remains system, and all other preferences match the saved originals. The temporary private backup was removed after restoration was verified. Direct human tray-menu selection is not claimed as automated proof.
- Repeated native application quit/start cycles completed during these checks. No Token database was replaced, deleted or edited by the agent; no real usage value or personal screenshot was persisted.
- Automated Token gestures left the app's saved position/size unchanged, but the original-header drag positive control also produced no recorded displacement. This automation result is inconclusive and is not labeled a product pass or failure. Asked specifically about Hover, title-region dragging and Token-region drag isolation on the currently running final app, the user replied **正常**, confirming all three. These are user-reported native observations on the verified final package. **Final macOS Package Smoke = Passed**, combining the tool-assisted checks with that user confirmation; at package-smoke time, full final-candidate human acceptance remained **Pending**; the later user-supplied Passed result is recorded below.

### Final Windows CI package smoke

The same successful Windows Job passed **NSIS install, uninstall registration lookup, executable discovery, launch, 8-second survival, process termination and uninstall**. The step result and actual workflow log were inspected. Installer generation and artifact upload passed afterward.

**Windows Final Candidate CI Smoke = Passed. Windows Final Candidate Human Acceptance = Pending at CI-smoke time; subsequently Passed by user confirmation below.** The earlier user-confirmed Windows fix pass is preserved separately and does not transfer to this new EXE.

### Current-state wording and frozen scope

The repository-wide wording review covered Windows Human Acceptance, Windows Fix Human Validation, Pending, old build sources and old Run IDs. Remaining old pending results are explicitly scoped to historical evidence; older feedback and implementation state names are unrelated to this release and were retained. At that preparation-stage wording review, Windows fix status was Passed and final-candidate human results remained Pending; the subsequent user-supplied acceptance is recorded below. All historical hashes and Run/Job/Artifact IDs were retained.

Only the two approved release documents changed in the build-source commit. Production code, tests, assets, scripts, dependency manifests and locks, Tauri configuration and workflows are frozen at `9281644`. `.github/workflows/ci.yml` retains `workflow_dispatch`; the release workflow is unchanged. No binary, credentials, real Token/Quota values, private runtime settings, SQLite file or machine-specific path is included in Git changes.

### Final rebuild local quality gates

All gates ran in the new clean final-rebuild worktree before the build-source commit. Only the two approved release documents changed. The existing temporary Rust toolchain and an isolated npm cache were used; no dependency, test, production, asset, script or workflow change was needed.

| Gate | Local result |
| --- | --- |
| `npm ci` | Passed |
| `npm run check:updater-policy` | Passed |
| `npm test` | **127 passed / 12 files / 0 failed** |
| `npm run build` | Passed |
| `npm audit --audit-level=high` | **0 vulnerabilities** |
| `npm audit --omit=dev --audit-level=high` | **0 vulnerabilities** |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` | Passed |
| `cargo test --manifest-path src-tauri/Cargo.toml --locked` | **73 passed / 2 ignored / 0 failed** |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings` | Passed |
| `git diff --check` | Passed |

The two ignored Rust cases remain the existing crash-test child helper and opt-in 1 GiB performance test. The preceding 541-case synthetic UI result is user-supplied historical evidence, not a claim that this separate harness was rerun here. `.github/workflows/ci.yml` retains `workflow_dispatch`; `.github/workflows/release.yml` remains byte-identical to the verified production/UI baseline.

### Final Actions quality gates

Both platform jobs passed npm ci, updater policy, version/source recording, frontend tests, frontend build, full and runtime audits, Rust fmt/tests/clippy, installer creation and artifact upload. Both audits found **0 vulnerabilities** on each platform. Frontend: **127 passed / 12 files** on each. Rust: **macOS 73 passed / 2 ignored / 0 failed**; **Windows 69 passed / 2 ignored / 0 failed**, reflecting the platform-specific suite. The different Windows count is not an ignored failure or a reduced threshold.

## Human acceptance

- **macOS Final Candidate Human Acceptance: Passed**
- **Windows Final Candidate Human Acceptance: Passed**
- **Explicit Release Authorization: Yes**

On 2026-09-11, the user explicitly confirmed both final-candidate acceptance results as Passed and authorized the official Codex Monitor v1.1.0 release. This is human-supplied acceptance evidence for the final Run #34571067271 DMG/EXE and their frozen hashes below; it is not inferred from CI, package smoke, older macOS acceptance, or Windows fix validation. No hardware details, OS build, screenshots, response timings or personal usage data are asserted.

## Release Closure

| Item | Verified release evidence |
| --- | --- |
| Release | Codex Monitor v1.1.0 |
| Tag | `v1.1.0` (annotated) |
| Tag Object SHA | `32a182b10c3a1489700db5093240dd6088adfce5` |
| Tag peeled target / FINAL_RELEASE_BUILD_SOURCE_SHA | `0eee3588197ef42298ab75bf945f62a226159ff3` |
| FINAL_RELEASE_EVIDENCE_DOCS_SHA | `d1a1c6b3522871aab82162272fa6c3c0bd786ec1` |
| Release ID | `386889946` |
| Release URL | https://github.com/bennick1/codex-monitor/releases/tag/v1.1.0 |
| Published At | `2026-09-11T08:35:29Z` |
| Draft / Prerelease | `false` / `false` |
| macOS Final Human Acceptance | Passed — user supplied |
| Windows Final Human Acceptance | Passed — user supplied |
| Explicit Release Authorization | Yes — user supplied |
| SHA256SUMS | Released and post-download verified |
| Post-release SHA256SUMS | Passed — both installers OK |

| Asset | Release Asset ID | Bytes | SHA256 |
| --- | --- | --- | --- |
| Codex-Monitor-1.1.0.dmg | `556838699` | 12,672,890 | `008fe507223c928f0732ec640a385cfaa7c2b7b9139b59927e8ad377b9d5c9ad` |
| Codex-Monitor-1.1.0.exe | `556838704` | 4,590,003 | `a9920fcc8a626f5198f7b13e1dd9882beab0097c50cfa01b911065df177df0ee` |
| SHA256SUMS | `556838697` | 180 | `ffdb036d8aa5ade166c71b49fb6d8db1ef5a1b85b195b774f51a580e81299648` |

The release contains exactly these three custom assets. Both installers and SHA256SUMS were downloaded from the official GitHub Release into a new empty verification directory separate from the upload source. Installer byte sizes and both frozen SHA256 values matched; the downloaded SHA256SUMS matched the uploaded manifest byte-for-byte and `shasum -a 256 -c SHA256SUMS` returned OK for both installers. Release metadata confirms the title, tag, non-draft and non-prerelease status. The remote peeled Tag target was checked after publication.

Historical Candidate A (`36bcd2c` / Run #34306184431) and Historical Candidate B (`0e7ca35` / Run #34454590766) remain Superseded. Their original evidence is retained above; no old installer was used. No rebuild, Release Preparation rerun, production/workflow/dependency/version/identifier change or branch cleanup was performed.

The closure commit is docs-only and is not the Build Source or Tag target. Advancing main with this documentation intentionally leaves v1.1.0 pointing at the frozen Build Source.

## Final state

**Released — v1.1.0**
