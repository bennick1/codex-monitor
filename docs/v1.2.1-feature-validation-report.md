# v1.2.1 feature validation

## Fast Display Bug Fix Decision

- Fast Display is an existing unfinished requirement, completed in this bug fix;
  it is no longer Deferred. v1.2.1 includes both newest-first ordering and Fast.
- `a794645ebdd3a9fd2fec7f2d55c32e77891a95df` is **Superseded — Sorting-Only
  Candidate**. Its DMG, EXE and hashes must not be used for final v1.2.1 release.
- `priority = Fast`; `default = explicit Standard`. Exact persisted per-Turn
  service tier has priority. The current installation does not expose one on the
  inspected persistent metadata surfaces, so this version uses the explicitly
  authorized conservative **Thread Timeline Fallback**.
- Lightning means best persisted local **selected/requested** evidence. It does
  not mean backend execution confirmed. If a client uses an unpersisted
  `serviceTierForTurn` override, the fallback may not observe that override.
  **Fast lightning reflects the best persisted local selection evidence available.**
- Schema 4 adds only nullable `model_turns.fast_mode` and sticky
  `fast_conflict`. Accounting parser Version 1, token facts, attribution for
  accounting, quota observations, provider period rules and ordering are frozen.
- Phase 0 / Phase 0B below remain historical research, including their per-turn
  override limitation and then-applicable gate. Their old blocked/deferred
  decision is superseded by the product decision in this section.
- Release boundaries remain: no main merge, v1.2.1 tag or GitHub Release until a
  separately authorized closure after Mac A/B, Mac restart and Windows installer
  human acceptance.

## Current installed source inspection — 2026-09-21

Read-only inspection identified Desktop **26.915.31945 (9922)** and its running
bundled **codex-cli 0.155.0-alpha.9.2**. No upgrade/downgrade, settings/session
write, daemon, long-running app-server or socket connection was introduced.
Local `app-server generate-ts --experimental` generated temporary protocol types
without launching a server.

- Installed `Turn` exposes identity/status/items/timing, without a service tier.
  `TurnStartParams` exposes both `serviceTier` and `serviceTierForTurn`; request
  capability is not evidence of persisted Turn settings.
- Read-only SQLite schema inspection: `state_5.sqlite` thread metadata and
  `thread_history_1.sqlite` exact thread/Turn index have no tier column. Queue
  requests do not establish stable completed-Turn tier and were not adopted.
- Frozen lexical metadata projection at 2026-09-21T03:26:42Z: **388 rollouts**,
  **2,785 contexts**, **2,056 settings** (**183 priority / 1,873 default**),
  **0 projection errors**. Only observed tier path:
  `event_msg → payload.type=thread_settings_applied →
  payload.thread_settings.service_tier`; ownership is `payload.thread_id`.
- All-envelope timestamp audit: **141,140 lines**, no decreasing timestamps.
  Owner guard remains necessary: 40 files have multiple session identities,
  43 declare a fork, and 24 Turn IDs recur across files.
- No reliable exact `turn_id + service_tier` pair was found in these inspected
  persisted sources. This is bounded evidence about this installation, not a
  claim about every future Codex version or arbitrary non-metadata content.
- Excluded message/prompt/reasoning/tool/source-code JSON is skipped by lexical
  projection/RawValue parsing. Reports retain only aggregates. Temporary source
  analysis hashes identities; real sessions and production databases are unmodified.

## Fast implementation contract

A dedicated metadata projection reads envelope ordering and the settings owner /
service tier. `parser.rs`, `normalize.rs`, the accounting schema and rule version
remain unchanged. Settings events remain ignored by accounting. The metadata
checkpoint serializes thread state, allowing incremental scans and restart.

`priority` selects true; `default` selects false; absent, malformed or other tier
selects unknown. Only matching explicit owner settings establish state. State
carries to subsequent contexts in the same reliable file-local logical thread.
A setting change does not retroactively update earlier Turns. Repeated contexts
with explicit true/false disagreement set `fast_conflict=1` permanently and the
API returns null. A missing observation does not contradict prior proven local
selection evidence. Separate files do not speculatively share timeline state.

Repeated session metadata, compaction/rollback boundaries, malformed records,
owner mismatch and decreasing timestamps clear carry-forward. A changed session
identity makes subsequent evidence in that source untrusted. Parent settings do
not establish a child's state; a child needs its own matching setting. Existing
accounting ownership and exact `codex-auto-review` hiding are unchanged.

Schema migration is transactional: Fresh/1/2/3 → 4. Both columns have value
constraints; only `model_checkpoints` is cleared for metadata replay. Accounting
cursors, token identities/facts and quota snapshots are retained. Versions >4 are
rejected before database mutation. Migration failure rolls back DDL and checkpoint
changes together.

The UI uses one 12px filled Phosphor Lightning inside the existing Effort cell,
with inherited text color and nonshrinking width. Effort alone can ellipsize.
False/null shows no icon. Chinese/English cell labels include Fast mode and unknown
effort. Four columns, 50px Effort, 306px widget and native geometry are unchanged.
Newest-first and thread/Turn ascending tie behavior remain unchanged.

## Fast verification and final candidate status

| Local gate | Result |
| --- | --- |
| Frontend red/green regression | 25 failing cases before implementation; final 204 passed / 14 files |
| Rust full suite | 118 passed, 0 failed, 2 existing ignored helpers |
| Fast backend cases | 13 new tests; plus ordering row-binding coverage |
| npm ci / updater policy / production build | Passed |
| npm audit (all / omit dev) | Passed, 0 vulnerabilities in both |
| cargo fmt / cargo clippy all targets, warnings denied | Passed |
| Browser layout | 24/24: 3 skins × 2 languages × 2 themes × Full/Compact |
| Geometry / branding | 306 CSS px, native geometry, icons and branding unchanged |
| Version / Identifier | All five sources 1.2.1; app.quotafloat.desktop |
| Accounting parser / normalizer / base schema | Unchanged |
| Independent Rust review | No actionable findings |

Backend regressions cover priority/default/missing/unknown/malformed values,
carry-forward, tier change, cross-thread/fork isolation, repeated same value and
sticky true/false conflict, reset/identity change/time rewind, incremental restart,
all migration paths, future schema rejection and failure at either ALTER or
checkpoint deletion. V3 replay preserves every accounting row/cursor, all Token
fields (including values above JS safe integer), fact count, By Model and quota
snapshot rows. The newest-first fixture runs both without Fast and with Fast on
03:00 / 01:00 and Standard on 02:00, asserting the entire row payload together.

Browser checks assert true/false/null icon binding, visible nonshrinking 12px SVG,
long effort ellipsis without icon clipping, inherited theme color, accessible
zh/en labels, four aligned columns and equal row heights with/without Lightning.
Screenshots were visually inspected across the six skin/theme combinations.
This is automated synthetic browser evidence, not native human acceptance.

Real-data checks use read-only production sources and an isolated temporary DB.
A current-period query used the actual provider reset returned by the Codex usage
tool (604800-second period): at the frozen query it found 8 eligible rows, including
1 Fast, 3 Standard and 4 Unknown, in newest-first order. No unknown row was guessed.
This does not identify the user's two historical screenshot rows, and is not a
controlled on/on/off A/B test.

The production metadata path was additionally exercised against **389** real
sources (the later scan includes newly created work), using a temporary isolated
SQLite database and frozen committed file prefixes. It restored **18 Fast /
833 Standard / 1,744 Unknown** metadata rows. Reconstructing schema 3, migrating,
replaying metadata only, and reopening the DB preserved the full metadata result,
all accounting tables/cursors, every aggregate checked, By Model and quota
snapshots. The helper also exercised the API with a clearly **synthetic rolling
seven-day period**, where 2 eligible Fast rows were returned; that count is not
the actual provider-period count above. Validation passed in 485.53 seconds with
a debug test binary. The temporary helper was removed before final source gates;
its DB was deleted on completion, and no real records were committed.

Controlled Mac Fast on/on/off, native restart and Windows installer human gates
remain pending. They require ordinary user interactions in the new exact-SHA
candidate; no automated or historical evidence is recorded as their completion.

## Retained Phase 0 / Phase 0B Research

Status: **Blocked — Fast Turn Attribution Unconfirmed**

Fast value mapping is **Confirmed**. Conservative local structural pairing
also exists, including completed `priority` and `default` examples. However,
the upstream implementation explicitly permits a per-turn override that is
absent from the persisted thread snapshot. A uniquely adjacent snapshot does
not establish the service tier selected for that Turn. This is the remaining
blocker; the value mapping is no longer a blocker.

## Baseline and authorized scope

- Inspection date: 2026-09-20, macOS.
- Repository: `bennick1/codex-monitor`.
- Verified branch: `feat/v1.2.1-fast-quota-order`.
- Verified starting and ending HEAD: `b4c16662774b4a71532a8b658e0cf99c986886d1`.
- This existing, untracked report was preserved and updated. It was not committed.
- Only this report was changed in the repository. No commit, push, checkout,
  Schema migration, production implementation, sorting change, or candidate work.
- The task's final report-only restriction governs this run, including a
  hypothetical successful source gate. No production file or test was edited.

## Upstream Fast semantics

Official `openai/codex main` was checked read-only and pinned to
[`5c5308fc9a9ee789049d646ef11e5400384b9c6f`](https://github.com/openai/codex/commit/5c5308fc9a9ee789049d646ef11e5400384b9c6f),
committed at 2026-09-20T03:02:48Z. These are current upstream source facts;
they do not establish that the inspected Desktop runs that exact commit.

| Value / contract | Confirmed meaning | Official evidence |
| --- | --- | --- |
| `ServiceTier::Fast.request_value()` | `priority` | [config_types.rs L528–552](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/protocol/src/config_types.rs#L528-L552) |
| `from_request_value("priority")`, `from_request_value("fast")` | Both map to `Fast` | Same source |
| `SERVICE_TIER_DEFAULT_REQUEST_VALUE` | `default`, the explicit Standard routing sentinel; it suppresses catalog defaults | [config_types.rs L533–537](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/protocol/src/config_types.rs#L533-L537) |
| `flex` | `Flex`; outside this feature | [config_types.rs L528–552](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/protocol/src/config_types.rs#L528-L552) |
| UI catalog `id: priority` | Display name `Fast` | [models.json L159–165](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/models-manager/models.json#L159-L165) |
| UI Fast status | Checks `ServiceTier::Fast.request_value()`; commands use catalog IDs/names | [service_tiers.rs L37–41](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/tui/src/chatwidget/service_tiers.rs#L37-L41), [L83–100](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/tui/src/chatwidget/service_tiers.rs#L83-L100) |
| `TurnStartParams.serviceTier` | Updates the thread default used by the current and subsequent Turns, subject to a per-turn override | [TurnStartParams.ts L38–46](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/app-server-protocol/schema/typescript/v2/TurnStartParams.ts#L38-L46) |
| `TurnStartParams.serviceTierForTurn` | Overrides only the new Turn, without changing the thread default; omitted/null inherits | Same source |

`default` is a confirmed explicit Standard selection, not missing metadata.
Missing metadata remains unknown. Neither value proves backend execution.

## Why structural pairing cannot prove the requested tier

The following source chain is decisive:

1. `update_settings_if` commits thread configuration and constructs both its
   configuration copy and settings snapshot.
   [session/mod.rs L1906–1923](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/mod.rs#L1906-L1923)
2. `new_turn_with_sub_id_if` then applies `service_tier_for_turn` only to the
   Turn's configuration copy. It returns the original `commit.snapshot`.
   [turn_context.rs L1076–1112](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/turn_context.rs#L1076-L1112)
3. `turn_input` passes that snapshot to `emit_applied`; snapshot emission is
   conditional on a thread-settings update, independently of the per-turn tier.
   [turn_input.rs L139–178](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/turn_input.rs#L139-L178)
4. `emit_applied` publishes `thread_id` and `thread_settings: snapshot`, without
   the per-turn override.
   [thread_settings.rs L123–138](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/thread_settings.rs#L123-L138)

Consequently, both counterexamples are permitted by the source:

| Thread snapshot | Unpersisted per-turn override | Tier requested for the Turn |
| --- | --- | --- |
| `priority` | `default` | Standard |
| `default` | `priority` | Fast |

These are source-permitted counterexamples, not claims that a local sample
actually used an override. Both can satisfy the proposed six structural rules.
Thus the rules establish **snapshot-to-Turn structural association**, but do
not establish the Turn's effective requested tier. Confirmed effective
`priority → Fast Turn` pairs: **0**. Confirmed effective
`default → Standard Turn` pairs: **0**.

Upstream emits the settings snapshot before spawning the task, which emits
`task_started` before the observed `turn_context`. The observed order is
therefore consistent with upstream, but does not repair the semantic gap.
[turn_input.rs L340–363](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/turn_input.rs#L340-L363),
[regular.rs L48–51](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/tasks/regular.rs#L48-L51)

Only `event.msg` is persisted; the outer submission correlation ID is not part
of the settings event. Standalone settings updates and checkpoints can also
emit snapshots, so one snapshot does not inherently mean one new Turn.
[session/mod.rs L2551–2554](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/mod.rs#L2551-L2554),
[thread_settings.rs L20–28, L111–146](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/core/src/session/thread_settings.rs#L20-L146)

## Local structural inspection and privacy

The scope remained September active rollout files plus the two available
archived rollouts. File enumeration and per-file read lengths were frozen
before projection; projection completed at **2026-09-20T09:10:57.987Z**
(**17:10:57.987 Asia/Shanghai**). Later appends were excluded. This is a frozen
read of file prefixes, not an atomic application-wide transaction.

The old report retained aggregate counts only: 196 files, 957 settings
snapshots (`priority` 8 / `default` 949), and 1,162 contexts. The current read
found the counts below. The old eight records cannot be individually
reidentified from that aggregate report; all **16 currently visible priority
records** were inspected individually instead. Active diagnostic work and
other sessions continue to create metadata, so old and current totals must
not be conflated or forced to match.

| Current frozen scope | Count |
| --- | ---: |
| Files | 202 (200 active, 2 archived) |
| Settings snapshots | 966 |
| `priority` snapshots | 16 |
| `default` snapshots | 950 |
| `turn_context` records | 1,179 |
| File-local distinct context Turn IDs | 1,158 |
| `task_started` records | 1,151 |
| `task_complete` records | 1,113 |
| `turn_aborted` records | 10 |
| Projection errors / incomplete retained records | 0 |

These are structural record counts, including copied history; they are not
all unique real Turns. Repeated `turn_context` can occur after compaction.
[protocol.rs L3282–3344](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/protocol/src/protocol.rs#L3282-L3344)

The projection decoded only allowlisted event types, timestamps, sequence
positions, service tiers, model/effort, lifecycle metadata, and immediately
hashed thread/Turn identities. Excluded JSON values were lexically skipped,
without deserializing message, assistant, reasoning, tool, or source-code
bodies. No auth/JWT, Token quantities, quota values, raw IDs, raw session
extracts, or private source paths were retained in this report. Session files
were never written or copied. Temporary analysis used only sanitized metadata.
No application database was opened.

The inspected metadata covered settings, contexts, session metadata,
lifecycle events, and top-level `token_usage_record` metadata. No service tier
was observed in `turn_context`; its permitted projected fields were Turn IDs,
model, and effort. Model/effort and usage-event presence were inspected only
after identity/ordering analysis and never selected a tier or a pair.

## Pairing method and aggregate results

A structural pair required the same explicit logical thread owner, one
continuous file-local timeline, a verified preceding real `task_complete`,
then exactly one settings snapshot before both the target's first
`task_started` and first `turn_context`. Start/context must share an explicit
Turn ID. Conflicting tiers, missing identities, aborted/overlapping Turns,
uncertain history, and repeated same-value snapshots were excluded from A.
Both same-ID repeated contexts and completion were checked within that Turn.

Two independent implementations, one based on interval analysis and one on a
lifecycle state machine, produced the identical set of **815** structural
pairs. The following ledger is mutually exclusive and reconciles to 966:

| Settings-snapshot outcome | `priority` | `default` | Total |
| --- | ---: | ---: | ---: |
| A: unique structural pair | 2 | 813 | 815 |
| Repeated same-value, candidate-safe only | 2 | 26 | 28 |
| Mixed values during one lifecycle, Ambiguous | 1 | 3 | 4 |
| E: copied/forked history or missing owner | 10 | 30 | 40 |
| Other unbound | 1 | 78 | 79 |
| **Total** | **16** | **950** | **966** |

- Unique priority structural pairs: **2**, of which **1 completed** and
  **1 incomplete** at the frozen boundary.
- Unique default structural pairs: **813**, all completed.
- Ambiguous/non-promotable snapshots: **72** = 28 repeated + 4 mixed + 40 E.
- Unbound snapshots: **79**. No missing/ambiguous case becomes `false`.
- All 815 structural pairs still lack proof of the effective requested tier;
  none is approved for a production boolean under this gate.

Required A–E views use different units and are not additive:

| View | Count and definition |
| --- | --- |
| A | 815 uniquely associated settings snapshots / Turns; 814 completed |
| B | 15 snapshot-to-next-snapshot windows contain multiple distinct context Turn IDs; none qualifies as A; no carry-forward used |
| C, pre-start interval | 0 Turns have different explicit tiers between a verified preceding terminal boundary and their first start/context |
| C, full lifecycle diagnostic | 1 lifecycle contains mixed `priority`/`default`, accounting for 4 snapshots; its priority snapshot is Ambiguous |
| D | 269 file-local context Turn IDs (274 context records) have no snapshot in the previous-terminal-to-first-start/context interval |
| E | 40 excluded snapshots: 31 in 30 explicitly forked/copied files, plus 9 without a thread owner (4 active, 5 archived) |

The 28 repeated same-value snapshots span 8 otherwise eligible Turn intervals.
They remain candidate-safe only, not Passed. A recent or repeated setting is
never automatically preferred.

## All priority structural sequences

`Fnnn` are opaque file labels, not paths. `#n` is a file-relative record
position. All identities below are truncated SHA-256 pseudonyms. The sequences
omit bodies and quantities. “None” means not observed in the frozen file
prefix; it does not predict future events.

| File / priority position | Previous boundary, settings, next context/start and terminal | Next settings | Outcome |
| --- | --- | --- | --- |
| F017 #37 | No previous real terminal in this file. `#3 start → #9 context(c5ac539703a6cf0d) → #37 priority → #38 default → #42 same context → #43 default → #323 complete` | #38 default | Ambiguous, in-flight mixed settings; no new Turn |
| F150 #488 | `#487 complete → #488 priority`; no later start/context/terminal | None | Unbound |
| F151 #771 | `#770 complete → #771 priority → #772 priority → #773 start → #776 context(a3b00e62933c2e66)`; no terminal | #772 priority | Repeated same-value candidate-safe, incomplete |
| F151 #772 | `#770 complete → #771 priority → #772 priority → #773 start → #776 context(a3b00e62933c2e66)`; no terminal | None | Same candidate, not a second pair |
| F188 #10 | No previous real terminal. Copied parent `#3 start/#7 context → #10 priority → #11 child start → #14 context(7e1cf158dedd93f3) → #39 complete` | None | Fork/copied history; excluded |
| F190 #10 | No previous real terminal. Copied parent `#3 start/#7 context → #10 priority → #11 child start → #14 context(0e044d649b3fec90) → #47 complete` | None | Fork/copied history; excluded |
| F192 #10 | No previous real terminal. Copied parent `#3 start/#7 context → #10 priority → #11 child start → #14 context(d25a28f84d0c1586) → #58 complete`; another Turn `#59 start/#60 context → #79 complete` | None | Fork plus multiple subsequent Turns; excluded, no inheritance |
| F193 #17 | `#15 complete → #17 priority → #18 start → #19 context(85b4354cdaa0c708) → #25 token_usage_record → #27 complete` | None | Unique completed structural pair; requested tier unconfirmed |
| F194 #301 | `#300 complete → #301 priority → #302 start → #303 context(bed8fb55ba149bc3)`; no terminal | None | Unique incomplete structural pair; requested tier unconfirmed |
| F195 #10 | No previous real terminal. Copied parent `#3 start/#7 context → #10 priority → #11 child start → #14 context(2f6459eab6ab324c) → #71 complete` | None | Fork/copied history; excluded |
| F196 #11 | No previous real terminal. Copied parent `#3 start/#7 context → #11 priority → #12 child start → #15 context(f759504604e156a1) → #94 complete` | None | Fork/copied history; excluded |
| F198 #17 | Copied parent `#12 complete → #13 start/#14 context → #17 priority → #18 child start → #21 context(d755498e70212edc) → #43 complete` | None | Copied terminal is not a new owner boundary; excluded |
| F200 #10 | No previous real terminal. Copied parent `#3 start/#7 context → #10 priority → #11 child start → #14 context(7fac140ef58ee8b0)`; no terminal | None | Fork/copied history; excluded |
| F201 #1440 | Archive: `#1439 complete → #1440 priority → #1441 priority → #1442 priority → #1443 start(7d63818f5cb6d8c2) → #1445 aborted`; no target context | #1441 priority | Missing settings owner, repeated snapshots, aborted; excluded |
| F201 #1441 | Same archived sequence; this is the middle of three priority snapshots | #1442 priority | Same excluded interval |
| F201 #1442 | Same archived sequence; next lifecycle is start then abort, without context | None | Same excluded interval |

## Positive and negative samples

### Completed priority structural sample

- Settings: `priority`, F193 #17 at 2026-09-20T08:43:50.852Z.
- Thread hash: `00d23f832f134fdb`; Turn hash: `85b4354cdaa0c708`.
- Previous real completion: #15 at 08:42:49.537Z.
- `task_started`: #18 at 08:43:50.855Z.
- `turn_context`: #19 at 08:43:50.861Z.
- Model: `codex-auto-review`; effort: `low`.
- Usage-record identity: #25 matches the same thread and Turn; no quantities read.
- Completed: **Yes**, #27 at 08:43:59.204Z.
- Next settings snapshot: none observed.
- Unique structural association: **Confirmed**.
- Fast requested for this Turn: **Unconfirmed**, because a per-turn override
  is not represented in the thread snapshot.

This is an internal automatic-review sample, not a verified user-visible
Fast-positive Turn. Its model did not influence pairing. A parent/root
relationship alone is not copied-history conflict: this file has one session
owner and a verified preceding completion under that owner. The user-visible
F194 sample has a clean priority association but was incomplete at the read
boundary. No completed user-visible priority sample satisfied the full gate.

### Completed default structural sample

- Settings: `default`, F005 #1180 at 2026-09-03T02:46:25.124Z.
- Thread hash: `1877a29e7baa32eb`; Turn hash: `4db9e952525923da`.
- Sequence: `#1179 previous complete → #1180 default → #1181 task_started
  → #1182 turn_context → #1192 token_count → #1437 task_complete`.
- Model: `gpt-5.6-sol`; effort: `high`.
- Completed: **Yes**, 2026-09-03T03:05:49.998Z.
- Next settings snapshot: none observed in this file prefix.
- Unique structural association: **Confirmed**.
- Explicit Standard value mapping: **Confirmed**.
- Standard requested for this Turn: **Unconfirmed** for the same override reason.

Neither sample's model, effort, usage counts, quota, or response speed was used
to infer Fast. Completion and model/effort attribution are structural facts,
not evidence about backend processing tier.

## Replay, archive, fork and identity stop conditions

- Never carry a setting across file boundaries or beyond the one proven
  immediate Turn interval. A B window does not mark every later Turn Fast.
- Reject copied/forked history when the active owner and provenance cannot be
  established. Upstream explicitly says copied settings retain their original
  logical owner.
  [protocol.rs L2193–2200](https://github.com/openai/codex/blob/5c5308fc9a9ee789049d646ef11e5400384b9c6f/codex-rs/protocol/src/protocol.rs#L2193-L2200)
- Do not splice contexts or completions by a bare Turn hash across files.
  Twenty-one context Turn hashes recur across files. Five A sources also have
  explicit fork copies; only their original, continuous source was eligible.
- One context hash occurs in eight different threads without its own start,
  completion, or settings. Those records provide no boundary evidence. A
  later verified real completion may establish a fresh interval; this does
  not retroactively validate the ambiguous prefix.
- One thread has two non-overlapping file segments. No setting or terminal
  boundary was carried between them.
- Archive location alone neither proves nor disproves continuity. In the
  observed archived priority case, missing owner, repeated settings, abort,
  and absent context independently stop attribution.
- Abort, rollback, timeline reset, missing/mismatched owner, overlapping Turns,
  unverified terminal boundaries, conflicting tiers, and nonmonotonic relevant
  ordering stop the current association. After reset, require a fresh,
  verified real terminal and a fresh pre-start snapshot; do not reuse history.
- Repeated same-value snapshots remain candidate-safe. Mixed values remain
  Ambiguous; no latest-value-wins rule was assumed.

## Per-turn override evidence

- Upstream supports `serviceTierForTurn`: **Yes**.
- Persisted local evidence: **Not observed**.
- Explicit metadata keys checked included `service_tier_for_turn`,
  `serviceTierForTurn`, `turn_service_tier`, `serviceTier`, `service_tier`,
  `fast_mode`, and `fast_requested` in the permitted metadata projection.
- The only observed service-tier field was
  `payload.thread_settings.service_tier` (966 snapshots).

**No persisted per-turn override metadata observed.** Upstream protocol
supports it, but no persisted evidence was observed in the inspected Desktop
rollout. This is not evidence that Desktop never uses the override, or that
Codex lacks support. In fact, the source explicitly omits the override from
the emitted thread snapshot. Absence therefore cannot establish inheritance.

## Product semantic and gate decision

Fast badge means **Fast requested / selected for this Turn**.

Fast badge represents the Codex-selected/requested Fast service tier,
not independent proof of the backend's final processing tier.

- UI text: `Fast`.
- Chinese tooltip/accessibility: `极速模式`.
- English tooltip/accessibility: `Fast mode`.
- Backend Fast execution proven: **No**.
- No accepted production rule currently establishes the requested tier from
  these snapshots. Even the intended request-only badge must remain deferred.
- If later implemented, `fast_mode` / `fast_requested` must describe requested
  or selected mode from persisted evidence, never backend execution.

| Phase 0B gate | Result |
| --- | --- |
| `priority → Fast` official mapping | Passed |
| `default → explicit Standard` official mapping | Passed |
| Completed priority sample, unique structure | Found, internal review sample only |
| Completed default sample, same structural rule | Found |
| Structural pairing independent of model/effort/Token/quota/speed | Passed |
| Explicit replay/archive/fork stop rules | Specified and applied |
| Structural rules establish the actual Turn request selection | **Failed: upstream supports an unpersisted per-turn override** |

The existence of a structural pair must not be substituted for a reliable
Fast/Standard label. **Phase 0B is not Passed.** The corrected final state is
**Blocked — Fast Turn Attribution Unconfirmed**.

## Historical Phase 0B production, validation and release state

| Area | Result |
| --- | --- |
| Production changes | **None** |
| Fast normalization / persistence / historical replay | Not implemented |
| Schema | 3 unchanged; no Schema 4 migration added or executed |
| Accounting parser | Version 1 unchanged |
| Token facts / quota snapshots / application databases | Untouched |
| Frontend / CSS / Fast badge / accessibility | Unchanged; feature acceptance not run |
| Backend and frontend sorting | Existing ordering retained |
| Tests / version files / README / CI | Unchanged |
| Build, frontend/Rust tests, fmt, clippy, audits | Not rerun for this report-only task |
| Local verification | Metadata allowlist projection, complete priority audit, two independent matching pairing calculations, count reconciliation |
| Report and workspace verification | Whitespace/privacy checks passed; tracked diff and index unchanged; this report is the sole untracked task file |
| Branding / icons / identifier | Unchanged |
| Existing v1.2.0 installation, release and tag | Untouched |
| Candidate artifacts / CI build / installation / main merge / v1.2.1 tag / release | Not performed |
| Commit / push | Not performed |

The previous Phase 0 release-safety facts remain retained: the existing
v1.2.0 tag was recorded as `a6e17f70ce3e2430f74ccbe7ccd7d025db6ba681`, and no
release or installation operation occurred in Phase 0B. No synthetic result,
package check, or structural audit is claimed as native human acceptance.

At the close of Phase 0B, possible next directions remained user decisions:
accept explicitly thread-level Fast semantics, release only Quota Week
newest-first, or wait for persisted per-Turn tier evidence (or another proven
effective-request source). None was selected or implemented within that
report-only phase. The final scope decision above subsequently authorized only
Quota Week newest-first.


## Quota Week Newest-First Implementation

- Backend: `completed_at DESC, thread ASC, turn ASC`.
- Frontend: `completedAt DESC`, sorting copied complete `TurnTokenUsage` objects.
- Tie handling: deterministic backend thread/Turn order; stable frontend sort
  retains equal-time input order without Model, Effort, Token, or Weekly tie-breaks.
- Only two production lines changed. Provider weekly period, reset and QueryAt
  boundaries, completed-Turn eligibility, unobserved history, the 15-minute
  observation window, null Weekly display, `quotaObservedAt`, partial markers,
  and exact Quota Week-only `codex-auto-review` filtering remain unchanged.
- Overview, By Model, four-column layout, CSS, skins, window geometry, branding,
  quota API, accounting, schema/migrations, and CI remain unchanged.
- Real data: **Passed — newest first**, including a new normal completed Codex
  Turn becoming the first native row. This is technical evidence, not human acceptance.

### Automated validation

| Check | Result |
| --- | --- |
| Frontend test-first | Before comparator change: 3 failed / 45 passed; after: 48 passed |
| Frontend full suite after `npm ci` | 180 passed / 14 files, Vitest 4.1.11 |
| Backend test-first | New newest-first case failed against chronological output; tie case passed |
| Backend focused suite | 82 passed, 2 existing ignored |
| `npm ci` | Passed |
| `npm run check:updater-policy` | Passed |
| `npm run build` | Passed |
| `npm audit --audit-level=high` | Passed, 0 vulnerabilities |
| `npm audit --omit=dev --audit-level=high` | Passed, 0 vulnerabilities |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` | Passed |
| `cargo test --manifest-path src-tauri/Cargo.toml --locked` | 105 passed, 2 existing ignored |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings` | Passed |
| `git diff --check` | Passed |
| Branding and icons against baseline | Unchanged |

Frontend regression exercises the actual `TokenUsage` composition with input
01:00 / 03:00 / 02:00 and rendered output 03:00 / 02:00 / 01:00. It checks Effort
`future-effort / — / xHigh`, Weekly `80% / — / 65.125%`, and every row's Model,
Token, completion/observation tooltip and partial marker. Equal-time cases keep
both tested input orders; caller-owned arrays remain unchanged.

Backend regression checks all returned fields for the same three-time ordering.
Four equal-time Turns cover both thread and Turn ascending ties with inverse
model/token values, repeated queries and restart. Existing regression assertions
were updated only where their expected row positions encoded the old order.
The two ignored Rust helpers remain unchanged and were not silently enabled.

The default shell lacked Rust; an isolated temporary stable toolchain was used
without modifying global shell configuration. The initial focused frontend run
used pre-existing dependencies; the required full suite used the lockfile restored
by `npm ci`. These are automated checks, not native human acceptance.


### Real-data native verification

The current source was built as a direct-entry macOS debug App using an external,
ignored validation configuration. Its isolated identifier was
`app.quotafloat.v121validation.ordering`; the tracked production identifier
remains `app.quotafloat.desktop`. Process-path and open-file checks confirmed the
fresh validation executable and its separate SQLite namespace. Production
preferences and database were not opened or modified by this validation.
The collector read real Codex sessions; both synthetic switches were disabled.
No session records or Token/quota values were fabricated or edited.

The native Quota Week view's first row matched the newest eligible completion
from an independent read-only query, including Model, Effort and Token identity.
A normal read-only Codex CLI Turn then completed successfully and persisted its
own real lifecycle metadata. After the first scan committed, the native view
updated automatically with this new Turn first. Its explicit thread identity was
resolved to the stored identity, and its Model, Effort, Token and completion
text matched the first row. The prior newest row followed it. The new row had a
real post-completion Weekly observation; older unobserved rows retained `—`.
Schema 3 and `PRAGMA integrity_check = ok` were verified read-only.

The fresh isolated database needed approximately six minutes for the existing
initial scan and metadata-repair attempt over the real source set. Before that
commit, the new Turn was correctly treated as not yet validated. No collector,
readiness deadline, service, parser or migration was modified to shorten this.
The isolated App was quit after verification. Real values, raw identities,
private source paths and native screenshots are excluded from this report and
Git. Local ignored evidence remains available for this task.

### Candidate preparation and acceptance boundary

- Implementation commit: `983394cee67ce825fdb8f6ff18a4f2597bdcf6ae`.
- Backend, frontend, real-data and full local regression gates: **Passed**.
- Version upgrade is permitted only after those gates; the following Candidate
  commit changes the five version sources to 1.2.1, including both root package
  entries in `package-lock.json` and only `codex-monitor` in `Cargo.lock`.
- Product remains `Codex Monitor`; identifier remains `app.quotafloat.desktop`.
- README continues to identify Current release v1.2.0 until a separate release.
- Existing CI must run on the exact final Candidate SHA. Frontend, Windows,
  macOS and the Windows GUI subsystem check must all pass, with both named
  unsigned artifacts present. Candidate source/run/asset hashes belong in the
  delivery's `BUILD-INFO.txt` and `SHA256SUMS.txt` after that source is frozen.
- If the known Windows startup-readiness test fails once, rerun only the failed
  job once without editing tests, timeouts, service or workflow; stop on recurrence.
- Deliver downloaded CI DMG/EXE bytes by copying/renaming only; no repackaging.
- Mac human acceptance remains required for newest first, a new Turn first,
  quit/restart, Overview, By Model, row correspondence and all three skins.
- Windows human acceptance remains ordinary install, launch, view, quit and
  restart, with a simple newest-first check when Quota Week is available.
- Fast is excluded from v1.2.1 acceptance. Mac and Windows human acceptance must
  precede any separately authorized main merge, v1.2.1 tag or GitHub Release.
