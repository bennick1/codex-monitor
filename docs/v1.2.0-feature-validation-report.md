# v1.2.0 feature validation

## Windows final current state — 2026-09-15

- **Windows Final Native Acceptance: Passed — explicit user confirmation of colleague real-machine validation.** Windows functional/native validation is complete by explicit human confirmation. No unrecorded per-item screenshots, counts, DPI, coordinates or quota values are inferred. Full business/native acceptance is not repeated.
- **Windows console finding: Superseded — Debug Validation Build Artifact.** Both v1.1.0 and pre-fix v1.2 use `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`: Release GUI / Debug Console. The first Windows validation used Tauri `build --debug --no-bundle`; its CMD window was existing debug-build behavior, not a v1.2 runtime, Quota Week, Branding or release-product regression.
- `afceadffc7742964d4316e6b1fc345573c3f0e3d` remains in Git history. Its all-Windows GUI attribute is restored to the exact v1.1.0 `main.rs` baseline in this closure. Release subsystem behavior remains GUI; debug development behavior returns to Console. No runtime console-hiding workaround is added.
- A Windows-only **Verify Windows GUI subsystem** CI step runs after the normal Tauri release build and before artifact upload. It reads `src-tauri/target/release/codex-monitor.exe` with PowerShell FileStream/BinaryReader, checks DOS/PE headers and PE32/PE32+ Optional Header bounds/magic, and requires Subsystem **2**. Missing/malformed files and any non-2 subsystem, including Console **3**, fail the step. Handles are disposed in `finally`; no third-party dependency or artifact naming change. Field layout follows the [Microsoft PE specification](https://learn.microsoft.com/en-us/windows/win32/debug/pe-format).
- **Windows Release Startup Smoke: Pending.** The user explicitly clarified that colleagues tested only the debug fixed build. After this closure's CI, use its `codex-monitor-windows-unsigned` release artifact and confirm: (1) launch from release artifact/installed entry, (2) no CMD/Console, (3) normal Tray, (4) normal Widget, (5) Tray Menu → Quit, (6) restart with no CMD. These are the only remaining Windows human checks; no full functional re-validation is requested.
- macOS Final Native Acceptance and Finder: **Passed**, carried forward from the recorded human acceptance. Branding: **FROZEN**; ICNS: **Corrected**; Quota Week Historical Contract and Auto-review: **Passed**. Fast and Manual Reset remain **Deferred**.
- Production scope is limited to restoring `main.rs` and adding the CI release PE gate. Version remains **1.1.0**. No business source, data/schema/provider/accounting, UI, assets, geometry, skins, hover/drag or macOS native behavior changes; no version bump, merge main, tag or release.

### Closure verification

Local full regression: **Passed**. `npm ci`, updater policy, frontend **179 passed / 14 files**, frontend build, full audit **0 vulnerabilities**, production audit **0 vulnerabilities**, `cargo fmt --all -- --check`, Rust **103 passed + 2 existing ignored**, `cargo clippy --all-targets --locked -- -D warnings`, and `git diff --check` all passed. Existing isolated npm cache and Rust toolchain were used; no dependency/toolchain upgrade or global configuration change. The initial Rust command had no default toolchain configured; selecting the already-installed stable toolchain for this process resolved that environment issue.

The complete `main.rs` matches `v1.1.0` byte-for-byte. Compared with starting source `5f9e5e3c38ec8e98e5293c3cb1119c64ee2e82de`, only `main.rs`, `.github/workflows/ci.yml` and this report change; all three version declarations remain 1.1.0.

Final-HEAD CI and remote sync are pending push at this documentation commit. The execution handoff must record the exact final SHA, CI Run ID, frontend/macOS/Windows results, GUI gate output and both unsigned artifacts. PowerShell execution on the actual Windows release EXE is a CI gate, not a local macOS result. Release smoke remains pending explicit real-machine confirmation.

**Final State: Blocked** — Windows release startup smoke is pending; readiness for version bump requires successful regression, final-HEAD CI/GUI gate/artifacts, remote sync and the six release smoke checks.

All Windows pending/blocked statuses below are historical and superseded by this current-state section. The diagnostic evidence is retained.

## Windows 控制台修复技术回归 — 2026-09-14

**历史状态：Superseded — Ready for Windows Group 1 Human Validation。** 用户在控制台问题记录后明确授权排查处理。本节更新该问题的技术状态；下方 P1 记录保留为修复前证据，Windows 人工三组验收仍未通过。

- 修改范围仅为 `src-tauri/src/main.rs` 的子系统属性：由仅在非 debug 构建时设置，改为所有 Windows 构建均设置 `windows_subsystem = "windows"`。保留 debug assertions 及既有 debug 菜单；不修改 UI、Branding、Token accounting、Quota Week contract 或版本。
- 本轮修复包基于 `e631a32affdf60b1f7398eb43a70013e4e08bed6` 加上述入口修改构建，对应修复源码提交 **`afceadffc7742964d4316e6b1fc345573c3f0e3d`**。原 Validation HEAD `59ff6f6...` 仅标识修复前的包，不作为修复包的 exact-source SHA；本报告随后单独提交，不改变该修复源码。
- 采用既有 Tauri `build --debug --no-bundle`，外部配置仍仅覆盖名称和独立 identifier `app.quotafloat.v120validation.winfinal`，版本保持 **1.1.0**。生产安装未覆盖，生产数据库未复制，synthetic quota 与 synthetic shortWindow 均 Off。
- 格式检查、debug 构建及 `git diff --check` 通过。旧 EXE 的 PE Subsystem 为 3（Console），修复后为 2（GUI）。
- 直接启动与重启后，存活进程的 `AttachConsole` 检查均返回 `ERROR_INVALID_HANDLE`，确认无关联控制台；测试辅助进程先解除自身控制台关联，避免将已有控制台导致的拒绝误判为通过。[Microsoft AttachConsole 文档](https://learn.microsoft.com/en-us/windows/console/attachconsole)说明了该错误语义。
- 原生托盘菜单的 `Test: simulate 5-hour quota` 仍存在；技术回归通过原生菜单命令 Quit 正常退出，再启动同一修复 EXE。交接时移除远程调试参数，直接运行修复版。
- 已确认用户当前使用的副本为独立 validation identifier，备份旧 EXE 后将该验证副本更新为已核验的修复文件；从原验证位置直接启动，再次确认无关联控制台。生产安装与验证数据不作替换。
- 上述为针对修复的技术回归，不能代替明确人工确认，尤其不作为 Group 1 Tray Menu 或 Group 3 Quit / Restart 的人工 Passed。Group 1 九项继续待用户确认，Group 2、Group 3 尚未开始。

## Windows Final Native Acceptance — 2026-09-14

**修复前 Windows 状态：Blocked — Windows Native Acceptance Failure。** 本节保留首次 Windows 交接的阻塞记录，当前状态以上方控制台修复技术回归为准；不改变已完成的 macOS 人工验收结论。当时仅完成技术准备并收到启动生命周期问题反馈，没有取得 Windows Group 1 通过确认，Group 2、Group 3 未开始。

### 源码与独立构建

- Tested Source SHA：`5359c705b43812564956a6cd78f5c524a327e326`。
- Validation HEAD：`59ff6f6ad3983ac20dfcbafe4e02796f1a76a964`；分支为 `feat/v1.2.0`，构建前工作区干净且与远端一致。
- 两个提交之间仅 `docs/v1.2.0-feature-validation-report.md`、`docs/v1.2.0-turn-quota-feasibility.md` 有差异，production source 等价。
- 保持版本 **1.1.0**。使用既有 Node 22.23.2、npm 10.9.8、Rust/Cargo 1.98.1，执行 `npm ci` 及正常 Tauri `build --debug --no-bundle`；未升级工具链或修改依赖锁文件。
- 外部配置仅覆盖 `productName` 与 `identifier`。运行时核实名称为 `Codex Monitor v1.2 Windows Final Validation`，identifier 为 `app.quotafloat.v120validation.winfinal`，版本为 `1.1.0`。
- 直接运行本轮 debug EXE，核对实际进程路径与构建文件一致，启动时仅有一个验收应用进程；未安装 NSIS、未覆盖现有生产安装。
- 独立 validation identifier 的旧验证数据原样移至带时间戳的旁目录保留；本轮 SQLite 在独立目录重新创建，运行时 schema 为 3。Preferences 同样使用独立 identifier。
- 使用真实 CODEX_HOME，源文件打开路径仅设置 `OpenOptions.read(true)`，未修改真实 JSONL 或文件权限；正常真实 Provider 路径，synthetic quota、synthetic shortWindow 均 Off。生产数据库未复制、未升级，现有生产 EXE 哈希保持不变。
- ICO 技术检查为 8 个 representation：16、20、24、32、48、64、128、256。Branding 与 Tray 资源未修改。容器检查及运行时元信息均不代替 Explorer / Tray / 鼠标交互的人工 Gate。

### P1 阻塞：debug 控制台关闭导致应用退出

| 字段 | 记录 |
| --- | --- |
| Gate | 启动方式 / 原生进程生命周期技术准备 |
| Steps | 直接启动本轮 validation EXE；出现附带命令行窗口；用户关闭该窗口 |
| Expected | 原生桌面验收不依赖额外命令行窗口；应用正式退出应通过 Tray Menu → Quit 验证 |
| Observed | 用户明确反馈启动后存在命令行窗口，关闭后应用进程随之退出，并提供截图 |
| 技术证据 | `src-tauri/src/main.rs` 仅在 `not(debug_assertions)` 时设置 `windows_subsystem = "windows"`；本轮 debug EXE 的 PE Subsystem 实测为 3（Windows Console） |
| 原因 | 当前 debug 构建关联控制台，关闭控制台会终止关联应用。该证据不表明 release 构建具有相同问题 |
| 处置 | 停止当前人工验收，未在本任务修改源码、EXE 或启动方式，未以隐藏控制台绕过问题 |

此前技术交接漏查了 PE 子系统及额外控制台窗口，不能据此认定原生生命周期已就绪。关闭控制台不作为 Tray Quit Gate 的通过证据。

### Windows 人工 Gate 状态

| 组别 | 项目 | 当前结果 |
| --- | --- | --- |
| Group 1 | Explorer App Icon、System Tray Icon、Tray Menu、Pure Hover ×5、Overview、By Model、Quota Week、Click Isolation、Internal Scroll | 九项均未取得明确人工通过确认；验收因上述 P1 暂停 |
| Group 2 | Window Drag、Compact Height、Full Height、Compact → Full、Full → Compact、Default、Blur、Computer | 未开始 |
| Group 3 | Tray Show / Hide、Refresh、Quit、Restart、Preference Persistence、Position Persistence、Quota Week Restart | 未开始 |

Windows Final Native Acceptance 未通过；不以 CI、浏览器、静态截图或技术检查替代人工确认。后续需单独处理验收构建的控制台生命周期问题，再交接 Windows Group 1。

### 保持项与本次提交范围

- macOS Final Native Acceptance、Finder：保留既有 Passed；Branding：FROZEN；ICNS：Corrected。
- Quota Week Historical Contract、Auto-review Filter：保留既有 Passed；Fast 与 Manual Reset：继续 Deferred。
- 本次用户明确授权提交并推送此阻塞记录；仅更新本验证报告，不记录 Windows Passed，不修改 production code、UI、Branding、Token accounting 或 Quota Week contract。
- 不执行 version bump、merge main、tag 或 release。技术日志和截图仅保留本地，不提交真实用量、额度、用户路径、用户名、账户信息或真实模型历史详情。

**Final State：Blocked — Windows Native Acceptance Failure。**

## Current-period historical Turn restoration — 2026-09-14

**Historical Turn Restoration: Passed. Final State: Ready for Windows Native Human Validation.** Automated regressions, real fresh-DB historical-row/restart checks and focused human re-validation passed. On 2026-09-14, the user explicitly confirmed all three handoff checks normal: current-period history with missing Weekly rendered as a dash; internal scrolling without outer scrolling/window drag; and a real task completed while the App was running with a legal percentage observation retained after normal exit/restart. Subsequent explicit human acceptance of all four native groups completes macOS final native acceptance as recorded below.

**Superseded — observed-only row eligibility.** All earlier statements that Quota Week returns only observed Turns, excludes a row when an observation is missing, or treats a dash only as malformed-IPC defense are historical and superseded by this section. Earlier acceptance of that data-set contract does not validate this change. Prior diagnostic sections and evidence remain intact.

### Source and new contract

- Clean starting `feat/v1.2.0` HEAD: `a2b571ad28dd2e750a836710fbfdc10a381ac21c`, matching the user's independently verified remote baseline. Only two documentation files differ from production baseline `116d97a8439050e686e8e1e1c21ac4684f2e238b`; fetch was not repeated as a hard prerequisite.
- Fix source: `5359c705b43812564956a6cd78f5c524a327e326` (`fix: preserve unobserved turns in quota week`).
- Quota Week = **current Provider quota week + all reliable completed Turn history + optional post-completion quota observation**.
- A valid seven-day Provider window is mandatory: `start = resetAt - 604800`; `start <= completedAt < QueryAt` and `completedAt < resetAt`. No natural-calendar fallback. Invalid Provider period remains unavailable.
- Existing reliable completion, metadata conflict/unknown handling, active response/legacy Fact ownership, unique identity, QueryAt limits and reconciliation are unchanged. A Turn without proven Token Facts is still excluded. Exact `codex-auto-review` remains hidden only from Quota Week; similar IDs and conflict/unknown follow existing rules. By Model retains auto-review.
- Observation matching remains the first snapshot in the same `weekly_reset_at`, with `completedAt <= observedAt <= completedAt + 15 minutes`, `observedAt <= QueryAt`, and finite remaining in `[0, 100]`. Valid observations retain their exact stored values. Otherwise **both `weeklyRemaining` and `quotaObservedAt` are null**, and the row remains visible.
- **Weekly historical unknown: Dash / null. No inference: Confirmed.** No current-value substitution, nearest observation fill, forward/backward fill, interpolation, Token-ratio calculation, quota monotonicity/reset-credit assumptions, or quota attribution to auto-review.
- Oldest completion first. Schema **3**, IPC, four columns, layout, `isPartial` and accounting `*` semantics unchanged. No new badge, warning, field, table, migration or historical marker.
- Snapshot writes still occur only after existing successful fresh Provider refreshes. Observation ledger code, cache-read behavior and HTTP cadence are unchanged; no completion-triggered or historical requests.

### Frontend

Empty valid period: **当前额度周暂无已完成记录** / **No completed turns in this quota week**. Unavailable period: **当前额度周不可用** / **Current quota week unavailable**.

Missing observation tooltip: **该轮完成后无可用的历史周额度观测** / **No historical weekly quota observation is available after this turn**. Accessible name: **周额度剩余：无历史观测** / **Weekly remaining: no historical observation**. The visible value is **—**. Observed percentage/time tooltips remain unchanged. Missing quota observations do not add accounting partial marks.

### Automated regression and accounting evidence

Test-first: **11 backend failures** reproduced against the prior row-filter implementation; **4 frontend failures** reproduced against the prior empty-state/accessibility text. After the minimal change:

| Check | Result |
| --- | --- |
| npm ci | Passed |
| npm run check:updater-policy | Passed |
| npm test | **179 passed / 14 files** |
| npm run build | Passed |
| npm audit --audit-level=high | Passed; **0 vulnerabilities** |
| npm audit --omit=dev --audit-level=high | Passed; **0 vulnerabilities** |
| cargo fmt --all -- --check | Passed |
| cargo test --locked | **103 passed + 2 existing ignored** |
| cargo clippy --all-targets --locked -- -D warnings | Passed |
| git diff --check | Passed |

Five new permanent backend tests cover fresh SQLite with three history rows across restart/rescan, mixed observed/unobserved chronology and persistent percentages, a current snapshot not filling yesterday's Turn, unobserved rollover, and exact auto-review filtering without observations while retaining By Model usage. Existing tests retain 0/15/16-minute boundaries, before-completion/cross-period/invalid/future observations, QueryAt Fact deferral, v2-to-v3 metadata backfill, lifecycle, ownership and reconciliation coverage. No tests were deleted and no timeout was changed.

A temporary same-fixture comparison executed the actual starting-HEAD query and the new query against the same synthetic SQLite. The old two observed rows exactly matched their new counterparts; the only added row had both quota fields null. Full Overview/By Model snapshots, accounting/Token Fact dumps, `model_turns`, `model_identities`, `quota_snapshots` and Schema 3 were identical before and after both queries. Temporary baseline code and local logs remain outside Git. Permanent restart tests also assert these invariants.

### Final direct-entry App and real data

Built the locked debug App through the normal current-source Tauri pipeline, with an external product name and a fresh validation identifier. Version remains **1.1.0**. `CFBundleExecutable=codex-monitor`; direct entry **Yes**; sole MacOS executable is `codex-monitor`; diagnostic/wrapper launcher **Absent**. `CFBundleIconFile=icon.icns`; resource SHA-256 **`8ccc7bddb8a1f120026a2174f8b5c1dc110b14a9862c38a0cd0f5ae09b5e7f61`**. Local ad-hoc strict deep signature verification **Passed**. Tray runtime still uses `tray-32.png`.

LaunchServices started the exact new executable. In-memory environment and open-file checks confirmed real CODEX_HOME, both synthetic switches Off and SQLite under the fresh isolated identifier. No production database was copied. Source confirms read-only Codex session input and normal Provider code. Native interaction checks below are technical checks, not human acceptance.

| Real-data gate | Result |
| --- | --- |
| Fresh isolated SQLite (absent before launch) | Passed |
| Current-period historical rows completed before App startup | Passed |
| Missing observations remain null | Passed |
| Historical rows render dash in native Quota Week | Passed — native AX |
| Normal Tray Quit and LaunchServices restart of same isolated App/SQLite | Passed — persistence scope only |
| Historical query results identical after restart at fixed QueryAt/period | Passed |
| Existing snapshot ledger retained | Passed |
| Unobserved history remains null/dash after restart | Passed — SQLite comparison and native AX |
| Live observed row percentage / survives restart | **Passed — explicit human confirmation**; deterministic mixed/restart fixtures also pass |
| Real new completion after App startup with legal observation | **Passed — explicit human confirmation** |

Only gate results are recorded here; no real Token amounts, quota percentages, model-history details, identifiers or screenshots are included. The failed initial technical AX navigation attempt was excluded from display evidence; subsequent native WebView inspection confirmed the history table and missing-observation display. The initial technical check had no eligible live observed row; subsequent explicit human confirmation closes that gate. No historical quota was fabricated.

### Native acceptance disposition

The user confirmed the preceding Group 1 six checks passed under the old contract. **Pure Hover ×5, Overview, By Model and Click Isolation remain Passed. Quota Week focused re-validation and Quota Week Internal Scroll: Passed — explicit human confirmation on 2026-09-14.** Group 1 is Passed under the new contract. The user explicitly accepted the Group 2 six-item handoff on 2026-09-14: **Window Drag, Left Dock, Right Dock, Top Dock, Bottom Dock and Corner Dock: Passed — human confirmation**. The accepted Dock checks include collapse → expand → collapse, correct anchors and safe insets, no overflow or drift, bottom expansion toward the screen interior and two-axis corner clamping. The user explicitly confirmed all six Group 3 checks normal on 2026-09-14: **Full-screen Space, Compact 314×460, Full 314×514, Compact → Full, Full → Compact and Bottom-docked transition: Passed — human confirmation**. The accepted same-process debug-menu transitions show/hide the 5h block without blank residue, clipping or position jumps, and preserve the bottom anchor. The user subsequently explicitly confirmed all five Group 4 checks normal on 2026-09-14: **Default, Blur, Computer, Quit and Restart: Passed — human confirmation**. Default has no old right-side decoration; Blur background/content are normal and clear; Computer retains background/stripes without the old GPT decoration. Real Tray Menu → Quit removes the widget and Tray; LaunchServices restart of the same validation App restores normal Tray/widget behavior, retained preferences and reasonable position. This explicit Group 4 confirmation is separate from the earlier quota-history persistence restart. No automated tests or builds were repeated for this documentation-only acceptance update.

### macOS final native acceptance

| Gate | Result |
| --- | --- |
| Group 1 — Basic interaction, including new Quota Week contract | Passed — explicit human confirmation |
| Group 2 — Window Drag / Dock | Passed — explicit human confirmation |
| Group 3 — Full-screen Space / Height | Passed — explicit human confirmation |
| Group 4 — Skins / Lifecycle | Passed — explicit human confirmation |
| macOS Remaining Native Final Acceptance | **Passed** |
| macOS Final Native Acceptance | **Passed** |

Acceptance uses the current production fix source `5359c705b43812564956a6cd78f5c524a327e326` and its direct-entry validation App; subsequent commits update documentation only. Earlier pending/blocked macOS statuses below are retained as historical evidence and superseded by this completed acceptance. This update changes only the validation report; no production source or frozen artifact was rebuilt.

**Next stage: Windows Native Human Validation.** Version remains **1.1.0**. Windows acceptance is still pending; only after macOS and Windows both pass may v1.2.0 Version Bump and Final Versioned Feature CI begin. No Windows acceptance, version bump, merge, tag or release is claimed here.

Finder, Tray Light, Tray Dark, Tray Menu and CM identity: **Passed**, carried forward. Branding: **FROZEN**; ICNS: **Corrected**; Dock icon residency: **N/A — intentional Accessory application**. Fast: **Deferred — Source Not Confirmed**. Manual Reset: **Deferred — No Observable Success Signal**. No version bump, merge, tag or release.

## Native ICNS production container fix acceptance — 2026-09-14

**Finder App Icon: Passed. Branding: FROZEN — Visual Identity Unchanged; Container Encoding Corrected.** The earlier **Frozen Branding Asset Conflict is Resolved** for the ICNS container. The historical Finder failure remains recorded below and is **Superseded — Native Decode Incompatible**: the custom writer produced native decode-incompatible small representations despite correct embedded PNG payloads. This was not a Finder-cache or bundle-copy error.

### Source and production container

Production fix commit: `116d97a8439050e686e8e1e1c21ac4684f2e238b`.

Started clean in the designated `feat/v1.2.0` worktree at `48ab24fbc09c6c597ce32f6f40e0905df2acb8b1`, matching origin after HTTP/1.1 fetch. The production ICNS was generated by the repaired generator, not installed by copying the accepted candidate. The custom ICNS chunk writer is removed. Apple `iconutil` consumes only the canonical 10-slot iconset copied from existing App PNGs; the temporary candidate passes native validation before installation. Tauri's intermediate ICNS never overwrites the checked-in native container. Non-macOS preserves an existing ICNS and explicitly fails before asset writes if it is missing.

| Container evidence | Result |
| --- | --- |
| Old SHA-256 | `7492301f162c18a2ff714147a1088aacf735ddb6a1fb64f1b05b599f17e7002a` |
| New production / accepted candidate SHA-256 | `8ccc7bddb8a1f120026a2174f8b5c1dc110b14a9862c38a0cd0f5ae09b5e7f61` |
| Container size | 946,051 bytes |
| Generator determinism | **3/3 byte-identical**, all three SHA values equal the new SHA above; one run exercised full asset generation |
| Canonical slots / unexpected 48 | **10/10 / Absent** |
| Small 1x representations | `ic04` 16@1x and `ic05` 32@1x, ARGB |
| Retina small representations | `ic11` 16@2x and `ic12` 32@2x |
| Raw strict slots | **8/8 Retina/larger slots exact** |
| Raw small differences | 16: 28 pixels; 32: 124 pixels; alpha and opaque RGB differences both **0** |
| Native NSImage render | **5/5 RGBA exact**: 16pt @1x/@2x, 32pt @1x/@2x, 128pt @1x |
| Native visible corruption | **Absent**; native source and candidate render arrays are exact |
| Frozen inputs | **17/17 SHA-256 unchanged**: reference, both SVGs, ten App PNGs and four Tray PNGs |

The earlier strict raw gate **Blocked — Native ICNS Round-trip Pixel Mismatch** remains historical. Raw 1x export differences were not waved through: difference localization, alpha/opaque integrity, premultiplied/composite analysis and exact AppKit rendering established **Native-Equivalent — Small 1x Legacy Re-encoding**. The user then explicitly confirmed **Finder Icon View: Passed; Finder List View: Passed**. **Human acceptance transferred from exact accepted candidate SHA.** This does not authorize arbitrary small-slot RGB changes: the repeatable validator requires raw alpha/opaque equality and exact native rendering, with strict equality retained for all eight other slots.

### Production-like bundle and Tray smoke

The normal locked Tauri debug App pipeline built `Codex Monitor ICNS Production Validation.app` from the updated feature worktree. Its external config contains only `productName` and `identifier`; icons were inherited with no override or post-build icon replacement. Bundle identity is `app.quotafloat.v120validation.icnsproduction`; `CFBundleExecutable=codex-monitor`, the sole MacOS executable; `CFBundleIconFile=icon.icns`. Resource SHA equals the accepted production SHA above. Direct entry: **Yes**; launcher: **None**.

After bundle contents were final, local ad-hoc signing and `codesign --verify --deep --strict` both **Passed**. This is local validation signing, not release signing/notarization. Finder acceptance transfers by exact accepted ICNS identity and the verified bundle icon key/resource.

One Direct LaunchServices Tray placement smoke **Passed**. The exact new main executable path was verified; its AX Tray frame `(962, 3, 36, 24)` lies within the observed menu-bar bounds `(0, 0, 2048, 30)`. The temporary process was closed and exit verified. Synthetic Off, real CODEX_HOME and SQLite isolated under the new identifier were verified without persisting private values or copying the production database. This smoke does not re-run human Tray readability/menu acceptance.

### Regression and boundaries

| Check | Result |
| --- | --- |
| npm ci | Passed |
| Updater policy | Passed |
| Frontend tests | **177 passed / 14 files** |
| npm run build | Passed |
| Full / production audit | **0 / 0 vulnerabilities** |
| cargo fmt --all -- --check | Passed |
| cargo test --locked | **98 passed + 2 ignored** |
| cargo clippy --all-targets --locked -- -D warnings | Passed |
| git diff --check | Passed |
| Full branding generator / validator | Passed: 63 PNGs, 8 ICO entries, 10 ICNS slots, 5 native render pairs |
| Old incompatible ICNS negative check | Rejected by canonical native representation gate |
| Simulated non-darwin generator branch | Full generation preserved accepted ICNS bytes; missing ICNS explicitly failed. This is branch simulation on macOS, not Windows/Linux native execution |

Tray Light, Dark, Menu and CM identity: **Passed**, preserved from prior human acceptance. Quota Week Auto-review, Real Data and observed-only semantics: **Passed**, preserved; no business/runtime logic changed. Fast: **Deferred — Source Not Confirmed**. Manual Reset: **Deferred — No Observable Success Signal**. Version remains **1.1.0**. Windows final human validation and other remaining native gates are still outstanding. No main merge, version bump, tag or release is included.

**Native acceptance outcome: Ready to Resume macOS Remaining Native Gates**, subject to the two commits being synchronized to the feature remote at delivery.

## Finder App Icon packaging isolation — 2026-09-14

**App Icon / Finder: Failed — original quota-week validation package**, explicitly confirmed by the user against `Codex Monitor Quota Week Validation.app`. Expected: frozen C + M + segmented monitoring arc. Observed: incorrect Finder icon. Other remaining native gates remain paused.

**Root Cause classification: Frozen Branding Asset Conflict. Final State: Blocked — Frozen Branding Asset Conflict.** The conflict is specifically between the frozen ICNS container's macOS-native decoding and its correct embedded PNG/source pixels. This does not establish an artwork geometry defect, nor prove the precise low-level encoding cause. No asset regeneration, production fix, cache reset or failure supersession was performed.

### Verified baseline and original bundle

Designated worktree was clean on `feat/v1.2.0`; HEAD and `origin/feat/v1.2.0`, after successful HTTP/1.1 fetch, were both `5ba5e517012fa01267eb6f23998f98c9926764bf`. Existing filter binary source remains `5ca0d87dfc0fe2284047a23ab6b19f87f1ea1957`.

| Old bundle field | Observed |
| --- | --- |
| CFBundleIdentifier | `app.quotafloat.v120validation.trayfix` |
| CFBundleExecutable | `codex-monitor`; sole MacOS executable, no diagnostic/wrapper launcher |
| CFBundleName / CFBundleDisplayName | `Codex Monitor` / `Codex Monitor` |
| CFBundleIconFile / CFBundleIconName | `icon.icns` / absent |
| CFBundlePackageType | `APPL` |
| CFBundleVersion / CFBundleShortVersionString | `1.1.0` / `1.1.0` |
| Contents/Resources | `icon.icns` present; 603,780 bytes |
| Repository vs bundle | **Byte-identical** |
| SHA-256, both ICNS files | `7492301f162c18a2ff714147a1088aacf735ddb6a1fb64f1b05b599f17e7002a` |
| Custom Finder icon metadata | **Absent**: no root FinderInfo attribute/custom-icon bit; no `Icon\r` file; only provenance xattr observed |

### Independent ICNS decoding

Read-only checks separately decoded repository and old-bundle ICNS with macOS `iconutil -c iconset` and Pillow PNG payload decoding. Both iconutil commands returned zero, but visual inspection revealed corrupted native small representations; successful exit is not a visual pass.

All 11 embedded PNG representations decode with Pillow and are pixel-identical to the corresponding frozen generated `app-N.png`: `icp4` 16, `icp5` 32, `icp6` 64, `ic07` 128, `ic08` 256, `ic09` 512, `ic10` 1024, `ic11` 32, `ic12` 64, `ic13` 256 and `ic14` 512. No approved source or generated asset was changed.

| macOS iconutil export | Actual dimensions | Comparison / observation |
| --- | --- | --- |
| icon_16x16.png | 16 × 16 | **Failed**: colored corruption; differs from frozen app-16 pixels |
| icon_32x32.png | 32 × 32 | **Failed**: colored horizontal corruption; differs from frozen app-32 pixels |
| icon_48x48.png | 48 × 48 | **Failed**: colored corruption; differs from frozen app-48 pixels; native export is 48 while the embedded icp6 PNG is 64 |
| icon_16x16@2x.png | 32 × 32 | Pixel-identical to app-32 |
| icon_32x32@2x.png | 64 × 64 | Pixel-identical to app-64 |
| icon_128x128.png | 128 × 128 | Pixel-identical to app-128; CM artwork visually confirmed in decoded output |
| icon_128x128@2x.png / icon_256x256.png | 256 × 256 | Pixel-identical to app-256 |
| icon_256x256@2x.png / icon_512x512.png | 512 × 512 | Pixel-identical to app-512 |
| icon_512x512@2x.png | 1024 × 1024 | Pixel-identical to app-1024 |

The native export discrepancy reproduces directly from the repository file, outside Finder and before any fresh App launch. It therefore cannot currently be attributed only to a stale Finder registration or an incorrect resource copy. The prior embedded-PNG technical validation remains true within that narrower scope; macOS-native ICNS visual correctness is not passed.

### Canonical Tauri / fresh Finder bundle

Built from verified current HEAD through the normal `npm run tauri -- build --debug --bundles app --config <external-config> -- --locked` pipeline, including the configured frontend build. The external JSON contains **only** `productName = Codex Monitor Finder Validation` and `identifier = app.quotafloat.v120validation.findericon`. Production icon configuration and plist icon keys were inherited unchanged. A new external target directory supplied the fresh App path; no old App copying, Contents assembly or executable replacement occurred. This one canonical build also supplies the requested fresh-identifier comparison. No matching application-support directory or preferences plist was present at the pre-build check; absolute historical non-use cannot be proven from those checks alone.

| Canonical / fresh bundle gate | Result |
| --- | --- |
| Normal Tauri build | **Passed**; generated `Codex Monitor Finder Validation.app` |
| CFBundleExecutable | `codex-monitor`; sole executable; no wrapper/diagnostic launcher |
| CFBundleIdentifier | `app.quotafloat.v120validation.findericon` |
| CFBundleName / CFBundleDisplayName | `Codex Monitor Finder Validation` |
| CFBundleIconFile / CFBundleIconName | `icon.icns` / absent |
| Resource | Present; 603,780 bytes; same SHA above; **Byte-identical** to repo and old bundle |
| Native ICNS visual gate | **Failed**; iconutil output files byte-identical to repository exports, including the small-size corruption |
| Custom Finder icon metadata | **Absent**: no root FinderInfo/custom-icon bit or `Icon\r`; only provenance xattr |
| codesign --verify --deep --strict | **Failed** on unmodified pipeline output: `code has no resources but signature indicates they must be present` |
| Human Finder result | **Pending / Needs Human Check**; not handed off because technical gates failed |
| Fresh runtime | Not launched; Synthetic/Provider/runtime isolation not newly claimed as verified |

The signature failure is an additional technical handoff blocker; its cause was not established or repaired, and it is not offered as the cause of the native-decoding corruption. Source checks retain the normal Provider path, default real Codex input and read-only session opens; the fresh identifier would isolate preferences/SQLite, but no runtime acceptance is inferred from source inspection.

### Scope and preserved acceptance

Production changes: **None**. Branding: **Frozen**. Direct Tray Light, Direct Tray Dark, Tray Menu, CM Tray identity, Quota Week auto-review filter and Real Data: **Passed**, preserved from prior explicit acceptance. Tray assets were not changed or revalidated. Version: **1.1.0**. Dock icon residency: **N/A — intentional Accessory app**. Fast: **Deferred — Source Not Confirmed**. Manual Reset: **Deferred — No Observable Success Signal**.

No frontend/Rust test-suite rerun, audit or clippy run; only the authorized canonical build, plist/resource/hash checks, independent ICNS decoding, metadata inspection, strict signature check and `git diff --check`. Tracked production files remained unchanged after the build. Local diagnostic JSON, native exports and build logs remain outside Git. Only this report is changed; other native gates remain pending. Next work requires explicit disposition of the frozen ICNS native-decoding conflict; this diagnosis does not authorize an asset or production configuration change.

## Quota Week internal auto-review filter and Direct Tray human acceptance — 2026-09-14

### Current human acceptance

The user explicitly confirmed the prior Direct-entry validation App's **Light Menu Bar: Passed; Dark Menu Bar: Passed; Tray Menu: Passed; CM identity: Passed** in this task brief. **macOS Direct-entry Tray Human Gate: Passed. Historical Tray Failure: Superseded — Diagnostic Harness Artifact.** These explicit human results supersede the pending four-item handoff in the historical paired-launcher report below.

This acceptance is restricted to the production-equivalent Direct-entry Tray scope. Pure Hover, edge docking, Fullscreen Space, height transitions, skins, Quit/Restart and other remaining macOS/Windows native gates retain their existing actual status. It does not mean all macOS Native Gates Passed.

### Production change and source

- Start: `057262d6e06a979ec3571ffea6ee9797fd8e0280`; designated clean `feat/v1.2.0` worktree verified before editing. The unrelated dirty primary checkout was untouched.
- Filter/test commit: `5ca0d87dfc0fe2284047a23ab6b19f87f1ea1957`.
- **Quota Week codex-auto-review filter: Passed. Filtered from Quota Week API/result only.** Six added production lines in `src-tauri/src/token_stats/turns.rs` define the exact model ID and skip a confirmed matching row immediately after metadata decoding, before fact aggregation or quota observation lookup.
- Conflict metadata still maps to `None` / unknown before eligibility. Unknown and similar IDs are preserved. There is no frontend filter, special message, preference, migration or accounting ownership change. Existing bilingual observed-turn empty states and internal scrolling remain unchanged.
- All existing current-Provider-period, completion-boundary, same-period observation, 15-minute tolerance, finite percentage and ascending-completion rules remain intact. Account-level Provider Weekly Remaining is never recalculated or attributed to a Turn.

### Tests and accounting regression

Four new backend tests cover mixed completed/observed turns, internal-only empty results, exact versus similar IDs, and conflict/unknown retention. Mixed-turn coverage also checks all Overview periods, By Model amounts, original ordinary-row observations, full fact/model/quota-history preservation, Schema 3 and restart/rescan consistency. Before production editing, three filtering tests failed on the extra internal rows and the conflict/unknown guard passed. After editing, all four pass. The existing frontend bilingual empty-state regression also passes.

An additional disposable comparison harness ran the original `057262d` query and the changed query against **the same persisted synthetic SQLite fixture**, at the same query time and weekly period. The complete accounting projection, all By Model periods (including Quota Period), accounting dump, model metadata and quota snapshot history are identical. The auto-review By Model row remains present. The only result difference is removal of the exact auto-review Quota Week row; ordinary Turn rows, including Weekly Remaining and observation timestamps, are identical. This harness and fixture remain outside Git. Scan timestamps/counters are operational metadata and are not mistaken for accounting differences in the separate restart/rescan test.

| Required regression | Result |
| --- | --- |
| npm ci | Passed; disposable npm cache; lockfile unchanged |
| npm run check:updater-policy | Passed |
| npm test | **177 passed / 14 files** |
| npm run build | Passed |
| npm audit --audit-level=high | Passed; **0 vulnerabilities** |
| npm audit --omit=dev --audit-level=high | Passed; **0 vulnerabilities** |
| cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check | Passed |
| cargo test --manifest-path src-tauri/Cargo.toml --locked | **98 passed + 2 ignored**, increased from 94 + 2 |
| cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings | Passed |
| git diff --check | Passed |
| Underlying accounting / model_turns / token_facts / quota_snapshots | Preserved |
| Overview / By Model exact Token values | Unchanged |
| codex-auto-review in By Model | Preserved |
| Provider Weekly Remaining | Unchanged |
| Schema | **3** |

The pre-existing isolated Rust installation needed explicit `RUSTUP_TOOLCHAIN=stable`; no global toolchain configuration or dependency change was made. One intermediate restart assertion compared scan metadata as well as accounting; it was corrected to compare complete accounting fields while retaining exact fact/model/quota dumps. No production timeout or test threshold was relaxed.

### New Direct-entry package and real-data focused validation

Built a new debug arm64 main from the filter commit with `tauri/custom-protocol`, locked dependencies and freshly built frontend assets. The isolated bundle uses **CFBundleExecutable=codex-monitor; Uses wrapper launcher=No**. Its sole MacOS executable is the new main; frozen icon bytes match the source, and strict deep ad-hoc signature verification passes. No diagnostic-launcher is present. The committed production identifier and version remain unchanged.

An initial UI connection resolved to the previous Direct-entry App because an older process used the same isolated identifier. Its observations were excluded from current-source acceptance. Only that exact path-verified old validation process was closed; the new package was then launched through LaunchServices and its actual executable path verified before accepting results.

For the verified new process, in-memory environment and open-file checks confirmed real CODEX_HOME, Synthetic Off, and SQLite under the isolated validation identifier, with no production database copied. The unchanged session reader uses read-only opens; the normal real Provider path successfully supplied current quota data. Native WebView AX inspection confirmed:

| Focused native check | Result |
| --- | --- |
| Quota Week auto-review hidden | **Passed**; current period has observed rows, with the internal model absent |
| By Model preserved | **Passed**; current Quota Period still contains the internal model |
| Real-data isolation / read-only session input | **Passed** |
| Direct-entry bundle and signing | **Passed** |
| Tray placement smoke | **Passed, one start**; after the user revealed the auto-hidden menu bar, the verified new process AX extras item was `(965,3,36,24)`, entirely inside `(0,0,2048,30)` |

No actual Token amounts, quota percentages, model-history detail or native screenshots are recorded here. Raw private UI content is not copied into repository artifacts. Prior four-item human Tray acceptance remains valid within its stated scope; no new Tray regression has been established.

**Branding: Frozen. Fast: Deferred. Manual Reset: Deferred. Version: 1.1.0.** No main merge, tag, release, schema change or version bump.

**Final State: Ready to Resume macOS Remaining Native Gates.** The single-start Tray placement smoke passed; the earlier three cold starts were not repeated. Tray Menu interaction retains the explicitly accepted production-equivalent human scope. Delivery uses separate filter/test and report commits, with ordinary HTTP/1.1 push and independent remote SHA verification.

## macOS Tray paired LaunchServices launcher A/B confirmation — 2026-09-14

**Harness Classification: Confirmed — Diagnostic Launcher / Bundle Entry Validation Artifact.** Direct LaunchServices full A7 was **5/5 In Menu Bar**; Historical diagnostic-launcher full A7 was **5/5 Off-screen**. The required paired repetition gate is met. These results identify a validation-harness artifact in the tested context; they do not establish an Apple internal mechanism or a production Tray placement defect.

**Final State: Ready for macOS Direct-Entry Tray Human Re-validation.** The new direct-entry real-data validation App passed **3/3 technical placement checks**. Light, Dark, Tray Menu and CM identity remain **Needs Human Check**. Remaining macOS native gates are not resumed until explicit human Tray acceptance.

### Git and exact build baseline

- Designated clean worktree: `feat/v1.2.0`; start HEAD and fetched remote were both `ebe7ebdbe9647147edba1668748f1555fd66a7ff`. Initial network attempts failed; the second bounded fetch retry in the resumed attempt succeeded. No branch switching, reset, merge or history rewriting was performed; the unrelated dirty primary checkout was untouched.
- `git diff --name-only 7ca42c4f49f59b5b9f82e124eb9f289925912410 HEAD` contained only this diagnostic report. All 269 tracked exported files were verified byte-identical to the starting source before this report update.
- Built **one** complete, unmodified production-startup main executable, debug arm64, with `tauri/custom-protocol`, `--locked --offline`. No startup stages, Widget, Token service, plugins, menus, event-loop policy or frontend commands were removed. No Rust diagnostic additions were made. This is full A7 behavior from clean source, without the prior Ready-time diagnostic print.
- Reused embedded frontend assets only after verifying all 44 tracked frontend/build inputs against their existing source export. No separate D/H rebuild occurred. The same successful main build also supplies H0 and the Direct validation App.
- Main Mach-O pre-sign SHA-256: `1b485bc67e60d2c90f3e5076dd8c79e4cf09b33300427bdbceae816c5ed12bd0`.

### Bundle parity and historical launcher audit

D and H share source, main executable before signing, identifier `app.quotafloat.v120validation.trayfix`, version **1.1.0**, LSUIElement=true, all remaining Info.plist values, frozen icon/resources/embedded frontend, window/plugin configuration, preferences behavior and database identity. Both use local ad-hoc signing and arm64. No defaults/preferences resets or forced state changes were made.

**Direct CFBundleExecutable:** `codex-monitor`. **Historical CFBundleExecutable:** `diagnostic-launcher`.

The exhaustive file/hash and plist comparison found only these allowed differences:

1. `CFBundleExecutable`.
2. H's additional `Contents/MacOS/diagnostic-launcher`.
3. Bundle/executable code signatures resulting from those content differences. Disposable audit copies of both signed main executables became byte-identical after signature removal.
4. The historical launcher's runtime environment mutations below.

**Unexpected differences: None.** Both bundles passed `codesign --verify --deep --strict` before launching. The previous round's actual historical launcher binary was reused; its identity against the older launcher was verified after removing signatures from disposable audit copies. Its source was audited without printing environment values.

Launcher Environment Mutations:

| Operation | Variable name |
| --- | --- |
| set / overwrite | `CODEX_MONITOR_TRAY_DIAGNOSTICS` |
| unset | `CODEX_MONITOR_SYNTHETIC_QUOTA` |
| unset | `CODEX_MONITOR_SYNTHETIC_SHORT_WINDOW` |

Main execution: **exec (`execl`)** of the sibling `codex-monitor`. The original mutations were preserved in H. No private values, credentials, usernames or real CODEX_HOME contents are included in this report.

### Five paired LaunchServices A/B trials

Every trial used `open -n` on the specified App bundle, never direct shell execution of main. Before each launch, both registered validation apps and leftover temporary validation processes were checked absent. The process executable path was verified against the target bundle. Each trial waited the same **8 seconds after observing the expected main PID**, revealed the auto-hidden menu bar, measured target AX, restored the pointer, terminated only that verified PID and confirmed its exit. The eight-second criterion replaces the older instrumented Ready-based settle criterion equally for every new trial.

One 2048 × 1280 display; revealed top menu-bar region `(0,0,2048,30)`. Every target exposed one AX extras menu item. Horizontal position varied by one pixel across direct trials; y/height and classification were consistent. No system processes were killed; no system defaults, ActivationPolicy, LSUIElement or positioning changes were performed.

| Pair | Variant, in execution order | PID | AX frame | Result |
| --- | --- | --- | --- | --- |
| 1 | D | 28653 | `(962,3,36,24)` | In Menu Bar |
| 1 | H | 28676 | `(-1,1268,36,24)` | Off-screen |
| 2 | H | 28699 | `(-1,1268,36,24)` | Off-screen |
| 2 | D | 28715 | `(963,3,36,24)` | In Menu Bar |
| 3 | D | 28740 | `(963,3,36,24)` | In Menu Bar |
| 3 | H | 28761 | `(-1,1268,36,24)` | Off-screen |
| 4 | H | 28781 | `(-1,1268,36,24)` | Off-screen |
| 4 | D | 28805 | `(963,3,36,24)` | In Menu Bar |
| 5 | D | 28828 | `(963,3,36,24)` | In Menu Bar |
| 5 | H | 28844 | `(-1,1268,36,24)` | Off-screen |

**Aggregate:** Direct **5/5 In Menu Bar**; Historical Launcher **5/5 Off-screen**. This meets the exact confirmation gate; no mixed or missing observations were promoted to success.

### Optional bounded mechanism isolation

After the 5-pair gate passed, H0 retained the same main, resources, identity and plist except the launcher entry. `diagnostic-launcher-noenv` performs only the sibling-main `execl`; it does not set, unset or overwrite any environment variable. Signature and main parity checks passed. H0 was **3/3 Off-screen**.

**Mechanism: Confirmed — Launcher Exec/Entry Path.** The launcher exec / bundle-entry path is implicated; environment mutations are not needed to reproduce this result. No H1 environment bisect or Apple-internal investigation was run.

### Production-like packaging and new Direct validation App

The existing normal Tauri debug bundle output's Info.plist was inspected: **Production-like CFBundleExecutable: `codex-monitor`; Uses diagnostic launcher: No.** This is an inspection of a normal generated packaging artifact, not a new release build. The historical diagnostic launcher is outside the production architecture.

Prepared **Codex Monitor Direct Native Validation.app** from the same one successful, full-current-source debug build. Its sole MacOS executable is `codex-monitor`; it has no launcher or wrapper. It keeps the isolated validation identifier above, frozen branding, embedded frontend, normal Provider code and separate preferences/SQLite. No production v1.1 database was copied or upgraded.

For each of its three cold starts, process environment was inspected in memory, persisting only boolean results: real CODEX_HOME matches, both synthetic switches are off. Open-file checks confirmed the process SQLite belongs to the isolated validation identifier and not production. Source inspection confirms real Codex session inputs use read-only opens; the Provider path is unchanged. These are real-data configuration/isolation checks, not a new acceptance of quota/API results during the unstable network.

| Variant | Cold start | PID | AX frame | Result |
| --- | --- | --- | --- | --- |
| H0 | 1 | 28879 | `(-1,1268,36,24)` | Off-screen |
| H0 | 2 | 28906 | `(-1,1268,36,24)` | Off-screen |
| H0 | 3 | 28933 | `(-1,1268,36,24)` | Off-screen |
| V | 1 | 28970 | `(963,3,36,24)` | In Menu Bar |
| V | 2 | 28995 | `(963,3,36,24)` | In Menu Bar |
| V | 3 | 29015 | `(963,3,36,24)` | In Menu Bar |

**Direct technical placement: 3/3 Passed.** Tray binding verification combines the unchanged source path (`TrayIconBuilder::with_id("main")`, bound production menu, frozen `tray-32.png`, tooltip `Codex Monitor`, successful builder registration) with the running main's single AX Tray item. It is source/startup/AX evidence, not a new debugger inspection of native object fields or human readability/menu-interaction acceptance. Bundle signature verification passed. All 16 measured processes exited; the new package is ready to open for human validation. Temporary package path, executable/file hashes and raw diagnostic records remain local and outside Git.

### Superseded historical attribution and human handoff

**Historical Tray Failure: Superseded — Diagnostic Harness Artifact.** Earlier `Blocked — macOS Native Final Acceptance Failure` and the Off-screen **production-failure implication** derived from historical-launcher validation are superseded. Their raw observations and all older diagnostic sections below are preserved as historical evidence with their original launch context. This section overrides their current-state/readiness implications.

This is **not “Tray Passed.”** Only the invalid production-failure attribution is retired. The user must check the new Direct App for:

- Light Menu Bar visibility/readability.
- Dark Menu Bar visibility/readability.
- Tray Menu opens.
- CM identity recognizable.

All four are **Needs Human Check**. Only explicit user approval makes macOS Tray Gate Passed and permits resuming remaining macOS native gates.

### Verification and delivery scope

Diagnostic main build, launcher-only H0 build, bundle parity, code signature verification, AX measurements and `git diff --check` passed. Full frontend/Rust tests, audits, clippy and CI were not run, as requested. Only this report is changed in Git; diagnostic scripts/artifacts remain temporary, and any reusable validation-packaging script belongs in a separate task.

**Production Changes: None. Branding: Frozen / Unchanged. Version: 1.1.0. Production identifier: `app.quotafloat.desktop`.** No workaround, dependency patch/upgrade, defaults operation, system-process restart, merge, tag, release or deployment was performed.

**Final State: Ready for macOS Direct-Entry Tray Human Re-validation.**

## macOS Tray full-process startup prefix isolation — 2026-09-14

**Root Cause: Native Placement Indeterminate. Final State: Blocked — Full-Process Trigger Still Indeterminate.** No adjacent production startup prefixes established a Good → Bad transition. A material diagnostic-launcher confound was observed; it must not be presented as a confirmed production stage or a production fix. This section supersedes earlier next-step conclusions, while preserving historical observations with their original provenance.

### Baseline, build parity and measurement

- Verified clean `feat/v1.2.0` start at `2b785f2a3b2f42af035f0c4151d9af63c0684236` in the designated worktree. The existing docs were pushed normally with HTTP/1.1; independent `git ls-remote` confirmed this exact SHA. The initial push could not update its local remote-tracking ref under the sandbox, but the remote update succeeded. The unrelated dirty primary checkout was untouched.
- Exported that exact source into temporary diagnostic storage. All formal trials used debug builds with `tauri/custom-protocol`, `--locked --offline`, the frozen image, Tray ID `main`, and one unchanged existing validation identifier. Reused frontend assets had matching frontend source and build configuration. Cargo manifest/lock, original Tauri configuration and Info.plist, and icon inputs remained byte-identical to the baseline. There were no dependency patches or upgrades.
- Phase A cuts only the Widget/native initialization suffix. Production plugin order, the complete Tray menu, event registrations, `prepare_app`, and the production Resumed/Exit callbacks remain a common execution shell. A7 has the complete production startup; its only Rust addition is the common Ready-time PID/native-policy diagnostic print. This common shell avoids accidentally treating a change to event-loop policy as a Widget-prefix effect.
- Actual isolated preferences had no preferences file: production defaults were `locked=false`, `always_on_top=true`. Existing isolated window state had `visible=true`. A2 was therefore N/A, and A7 really included the saved-visible show path. No preference or system defaults were forced or reset.
- Every formal trial was a fresh process. External orchestration waited four seconds after Ready, revealed the auto-hidden menu bar, read AX, restored the pointer, then terminated that exact inspected diagnostic process and verified its exit. No main-thread sleep, blocking human signal, AX production logic, or status-item recovery was inserted.
- System: macOS **26.6.2**, one **2048 × 1280** display. Revealed AX menu-bar region: `(0,0,2048,30)`. All formal trials reported native activation policy **Accessory (1)** and one main AX extras item. In-menu frame: `(1048,3,36,24)`; off-screen frame: `(-1,1268,36,24)`.
- Preliminary runs that omitted parts of the common event-loop shell or used the default development protocol were explicitly excluded. Their raw evidence is retained locally under invalid/preliminary labels; they do not count toward the matrices or repetitions below.

### Phase A — two launch contexts, never one combined boundary

The initial exported App used `CFBundleExecutable=codex-monitor`, launching the main executable directly. All seven applicable prefixes were In Menu Bar, including A7 with the entire production startup and embedded frontend.

Inspection then found that the historical off-screen diagnostic App used `CFBundleExecutable=diagnostic-launcher`, a small existing launcher that sets the diagnostic environment and execs the main program. Reusing that historical launcher and entry-point metadata restored the off-screen symptom in both the complete A7 program and A0. Product name, validation identifier, image, main-program source and preferences were unchanged between launch contexts. The pre-sign A7 Mach-O was byte-identical in the two cases (SHA-256 `1e9504ef376b286cab6eb067cc5dbe3d1ef9c64a0321b8e85156a63970e6139a`); A0 was also byte-identical across contexts. Bundle signing was repeated after changing the entry-point metadata, so final signed executable hashes differ and are recorded separately.

This is a **single-trial association with the diagnostic launch context**, not a confirmed startup-stage transition. It does not isolate the launcher's exec behavior, environment, bundle entry-point metadata, signing relationship, or system timing. No further packaging/identity experiment or production change was performed. Historical off-screen measurements cannot, by themselves, exclude a diagnostic-harness contribution.

| Variant | Last included Widget/native stage | Direct main executable | Historical diagnostic launcher |
| --- | --- | --- | --- |
| A0 | Full `setup_tray`; no Widget | In Menu Bar, 1 trial | Off-screen, 1 trial |
| A1 | `create_widget` | In Menu Bar, 1 trial | Not Run after A0 failure |
| A2 | Conditional `apply_lock` | N/A — actual locked=false | N/A — actual locked=false |
| A3 | `restore_state(POSITION | SIZE)` | In Menu Bar, 1 trial | Not Run |
| A4 | Production collection behavior flags | In Menu Bar, 1 trial | Not Run |
| A5 | `setHidesOnDeactivate(false)` | In Menu Bar, 1 trial | Not Run |
| A6 | Actual always-on-top/window level | In Menu Bar, 1 trial | Not Run |
| A7 | Saved-visible show, complete production startup | In Menu Bar, 1 trial | Off-screen, 1 baseline-reproduction trial before A0 |

**First Good Prefix:** A0 in the direct-entry matrix. **First Bad Prefix:** none in that matrix; A0 was already bad in the historical-launcher matrix. These are different launch contexts and cannot be combined into adjacent Good/Bad production prefixes.

### Phase B — fixed historical launcher, A0 already Off-screen

Following A0 failure in the reproduced historical launch context, A1–A7 were not advanced in that context. All B variants retained this same launcher, identity, frozen image, policy and normal event loop. Removals were cumulative from A0: B1 simplifies the menu, B2 additionally omits Token service startup, and B3 additionally omits reqwest/provider state. B3 manages a diagnostic preferences/path-only substitute; backend command implementations remain compiled but there is no Widget to invoke them. Each plugin group is then removed independently from B3, not cumulatively from the previous plugin-group trial.

| Variant | Difference from preceding baseline | AX result | Trials |
| --- | --- | --- | --- |
| B1 | Minimal Quit menu, full pre-Tray services/state/plugins | Off-screen | 1 |
| B2 | B1 without `TokenStatisticsService::start` | Off-screen | 1 |
| B3 | B2 without reqwest client/provider AppState construction | Off-screen | 1 |
| B4 minus P1 | B3 without opener and single-instance | Off-screen | 1 |
| B4 minus P2 | B3 without autostart and window-state | Off-screen | 1 |

Neither plugin group restored placement, so no group qualified for single-plugin isolation. No full-menu bisect was justified by B1. The bounded reverse-isolation branch is complete without a recovered prefix.

### Phase C, repetition and same-process probes

**Phase C Run: No.** Its required within-context condition did not occur: direct-entry A7 already includes the full production event registrations, prepare_app, Resumed refresh and embedded frontend and is normal; the historical-launcher context is already bad at A0 and takes Phase B. Comparing a direct-entry prefix against a historical-launcher full App would introduce a second variable. C1 event-handler grouping, C2 prepare_app removal, C3 Resumed split and C4 frontend command gating are **Not Run**.

- Qualifying Good Variant: **none**. Qualifying Bad Variant: **none**. Single production-stage Difference: **none**.
- Good repeat: **0/3**. Bad repeat: **0/3**. These are unperformed confirmation repetitions, not failed trials. Individual screening observations above are not promoted to a 3/3 trigger.
- Good-process AppKit probe: **Not Run**. Bad-process AppKit probe: **Not Run**. No production-stage boundary qualified for this optional secondary measurement.
- No claim of Apple-only failure, Tauri regression, Accessory-only defect, or a confirmed launcher mechanism is made. Native human visibility/menu-interaction acceptance remains outstanding.

### Verification and delivery boundary

All **12 formal diagnostic builds** passed: A0, A1, A3–A7, B1–B3 and the two plugin-group variants. The historical-launcher full/A0 trials reused the corresponding main binaries. Builds emitted expected unused-code warnings from omitted diagnostic stages; no full frontend/Rust suite, audit, clippy, or CI was run or claimed. Bundle signature verification and fresh-process AX observations passed as measurement prerequisites, not as production acceptance. All diagnostic processes were confirmed exited.

Temporary sources, exact prefix diffs, build logs, per-trial PID/policy/AX records, and pre-sign/post-sign SHA-256 manifests remain local and outside Git. Only this report is delivered. `git diff --check` passed; tracked production source, configuration, dependencies, branding and version files are unchanged. Production identifier remains `app.quotafloat.desktop`; version remains **1.1.0**. No production fix, merge, tag, release or deployment was performed.

**Final State: Blocked — Full-Process Trigger Still Indeterminate.** The requested single-production-stage criterion was not met. Any further attribution must first account for the observed diagnostic launch-context difference; this report does not authorize a production workaround.

## macOS Tray persisted-state / autosave identity isolation — 2026-09-14

**Root Cause: Native Placement Indeterminate. Final State: Blocked — Native Placement Indeterminate.** None of the requested identity experiments restored placement. The previously confirmed off-screen symptom remains reproducible; its cause is still unisolated. This section supersedes prior next-step/readiness statements below, which remain historical evidence.

### Provenance and method

- Clean start: `feat/v1.2.0`, `9397bdbdf16056632411e1b737c69fd91f33fca0`. The ordinary HTTP/1.1 push synchronized the existing docs commits; `git ls-remote` independently confirmed that SHA. The push could not update the local remote-tracking ref under the sandbox, but the remote update succeeded. No commits were recreated or history rewritten.
- Full product source was exported from that HEAD into temporary diagnostic storage. Original product code, frozen image/menu, Tray ID `main`, plugins, widget, startup order, Accessory policy and LSUIElement were retained. Native-only diagnostic additions and a direct reference to the already-locked AppKit/Foundation bindings remained outside the tracked worktree. No production source, dependency manifest/lock, configuration, database or version file changed.
- Each trial used the inspected, ad-hoc-signed diagnostic App bundle and a launcher enabling only diagnostic inspection. Every prior validation process was absent before launch; each trial process was terminated and its exit verified. Diagnostic preferences/database used the validation identifier, never `app.quotafloat.desktop`. Bundle metadata and executable hashes were recorded. Raw AX/native evidence and temporary source/patches remain local, outside Git.
- Measurements revealed the auto-hidden menu bar and restored the pointer. Screen bounds were `(0,0,2048,1280)` and the AX menu-bar region `(0,0,2048,30)`. Main-thread assertions guarded native access; only AppHandle crossed worker threads. External AX measurement used existing trust; no production AX integration or new permission request was added.
- Historical original failure **3/3**, and R1–R5 failures, are carried forward unchanged. R1–R5 were not repeated or combined with these identity tests.

### Current item and same-process control

| Property | Observed value |
| --- | --- |
| autosaveName | `Item-0` on all unmodified identity launches; automatic-looking AppKit value, not nil or an explicit application-supplied key |
| behavior | `0` |
| length | `-1.0` (variable length) |
| isVisible | `true` |
| statusBar | Present; equal to `NSStatusBar.systemStatusBar()` |
| button frame | `(0,0,34,22)` |
| Original revealed AX | `(-1,1268,36,24)` — Off-screen |

The locked tray-icon 0.24.1 implementation has no autosave setter in its creation path. The diagnostic getter tolerates nil despite the bindings' non-null return annotation; the actual runtime value was `Item-0`.

In PID **20092**, after externally confirming the main item's off-screen frame, a main-thread callback created a second item directly with public AppKit `NSStatusBar.systemStatusBar().statusItemWithLength`, title `CM-P`, variable length and no Tauri builder. The item was retained for the process lifetime. Main and probe existed simultaneously: AX extras child count changed from one to two. The unchanged main was `(-1,1268,36,24)`; the new text probe was `(-1,1268,57,24)`. **Both Off-screen (Case B)**; no main placement recovery occurred. This does not establish Tauri-created-item-specific state, nor does it prove a process/bundle cause by itself.

### Identity A/B and autosave results

| Experiment | Identity / intervention | Cold-start PIDs | Revealed main AX | Result |
| --- | --- | --- | --- | --- |
| Original identifier | `app.quotafloat.v120validation.trayfix`, Codex Monitor | 20092, 20333, 20407 | `(-1,1268,36,24)` each | Off-screen **3/3** |
| Fresh identifier only | `app.quotafloat.v120validation.traystate2`, same product/source | 20431, 20444, 20457 | Same each | Off-screen **3/3** |
| Fresh product name | Same fresh identifier, productName `Codex Monitor Tray Probe`; unchanged source | 20769 | Same | Off-screen **1/1** |
| Explicit autosave after creation | Original validation identity; set `app.quotafloat.codex-monitor.tray.v120-test2` after confirmed off-screen baseline | 20787, 21283, 22275 | Same before and after each setter | Off-screen **3/3** |
| Autosave at creation | Original validation identity; `app.quotafloat.codex-monitor.tray.v120-autosave-test` immediately after native item creation | 23919, 24923, 24939 | Same each | Off-screen **3/3** |

The fresh identifier was fixed across all its launches; no pre-existing preference-domain file was found before first use. The product-name comparison retained that identifier so that only product identity changed from the preceding fresh-identifier variant. No random per-launch identity was used.

For the after-creation experiment, every process initially reported `Item-0`. Only the public autosaveName setter ran, followed by a separately queued main-thread inspection and revealed AX measurement. Every inspection read back the exact requested key. No visibility toggle, rebind, recreation, deferred startup or native probe was combined with this experiment.

**Diagnostic upstream-style autosave support:** Phase 6 used a temporary local copy of tray-icon **0.24.1**, with the fixed public setter inserted immediately after `statusItemWithLength`, before icon/menu/target setup. A patch entry existed only in the exported diagnostic manifest, never the formal manifest. No initial production Tray was created and then replaced. All three launches read back the intended creation-time key. This reproduces the relevant setter timing, not the whole upstream API proposal, and it did not recover placement.

### Persistent state and upstream

**Read-only record: Unknown. Key relation: Unknown.** Limited freshness preflight found the old validation preference-domain file present and the fresh domain file absent. Neither explicit test key appeared among the inspected domain-file keys before use. File presence alone does not establish a status-item placement record. No identity experiment showed recovery or strong correlation, so the conditional Phase 7 broader NSUserDefaults/CFPreferences/system-domain investigation was not run. Absence in the limited preflight is not proof that all possible system records are absent.

No defaults write/delete, plist preference mutation, SystemUIServer restart or ControlCenter restart was performed. Public AppKit autosave setters were used only for the explicitly authorized experiments; any persistence AppKit itself performs is distinct from manual system-preference edits.

[tray-icon PR #317](https://github.com/tauri-apps/tray-icon/pull/317) was checked through GitHub's live API: **Open / Unmerged** (`state=open`, `merged=false`). It proposes an optional stable autosave name. **Used in production: No.** The current failed tests do not identify our bug with #317 or establish it as a fix.

### Build evidence and scope

All four full diagnostic App variants built successfully. `cargo check --locked --offline` passed for the unpatched diagnostic source and the temporary creation-time patch variant. The local tray-icon patch build/check emitted non-fatal compiler warnings; strict clippy/full CI was not run or claimed. `git diff --check` passed. Production source diff is empty; only this report is delivered.

| Launched diagnostic executable | SHA-256 after signing |
| --- | --- |
| old | `9c7d8778e89f5321772f9d1a99ff1ea84c224fcc5eb813dc82bfc71812124e59` |
| fresh | `98b542971ccb21d84c3f80ee9887560645f3b9cb77a28e40b87c87ee1e358bb2` |
| product | `d718064a85ab9c0cd0ac052a8ce47e20af708e256d3080867f293adfeb6e66bb` |
| creation | `b0b083dd4abf877501bbcbbca1c9af7b01b41395bbd09e8fb594848bb0e4f585` |

No production fix was selected. No dependency upgrade, Tray library replacement, branding/menu/policy change, system-state repair, release or version bump was performed. Production identifier remains `app.quotafloat.desktop`; version remains **1.1.0**. Native human acceptance gates remain outstanding. The authorized diagnostic sequence is complete; a minimal persisted-state fix is not supported by these results.

## macOS Tray placement recovery experiments — 2026-09-14

**Blocked — macOS Tray Recovery Not Found.** The original Off-screen Bottom failure reproduced in **3/3 cold starts**. R1–R5 each failed their first isolated trial. No candidate qualified for the five-cold-start repeatability stage, and no production workaround was implemented. The specific trigger remains unisolated; these results do not establish a Tauri regression or deterministic recovery.

### Baseline and isolation

- Clean `feat/v1.2.0` source: `539a687f93927bcb3f9a0cb4c4cb3bb05e91f574`. Local HEAD and remote-tracking reference matched. HTTP/1.1 fetch failed first with an empty server reply and then with a bounded low-speed timeout; a live GitHub Git-ref API read independently confirmed the same remote branch SHA before experimentation.
- A fresh exported source tree was used for diagnostic builds. The external override and inspected App bundle use `app.quotafloat.v120validation.trayfix`. Preferences resolve through the isolated identifier; the separate SQLite directory was verified. No production configuration or database was copied or changed. Existing validation instances were checked and each trial process was terminated before the next cold start.
- Current measurements: macOS 26.6.2 / 25G83, arm64 host, one built-in display, logical bounds `(0, 0, 2048, 1280)`, backing scale reported as 1.0, top AX menu-bar region `(0, 0, 2048, 30)`. Before every AX observation, the pointer briefly revealed the auto-hidden menu bar and was restored. No system setting was changed.
- Baseline binary SHA-256: `d39f5a6c6cb4a1851b426557abbb9f3122a7e238d83a6dd11cb4842c7fa04915`. Cold-start PIDs 15630, 15647 and 15661 each reported one AX extras child at **`(-1, 1268, 36, 24)`**.
- All three baseline native inspections reported main-thread=true, Tray ID `main`, registered NSStatusItem, image/menu/tooltip bound, button hidden=false and alpha=1.0. AX was used only by the external diagnostic measurement tools with existing trust.

### Ordered recovery results

| Candidate | Fresh process | Action | Revealed AX after action | Result / repeat stage |
| --- | --- | --- | --- | --- |
| R1 Visible Toggle | 15983 | `set_visible(false)` then `set_visible(true)` in a subsequent queued main-thread callback | `(-1, 1268, 36, 24)` | Failed; repeat not run |
| R2 Icon Rebind | 16287 | `set_icon(None)` then restore the unchanged `icons/tray-32.png` in a subsequent queued main-thread callback | `(-1, 1268, 36, 24)` | Failed; repeat not run |
| R3 Menu Rebind | 16594 | Detach menu then reattach a retained clone of the original Menu in a subsequent queued main-thread callback | `(-1, 1268, 36, 24)` | Failed; repeat not run |
| R4 Main-thread Tray Recreate | 17322 | Remove `main`, drop returned handle on the main thread, then rebuild one `main` with original Menu, tooltip and frozen image | `(-1, 1268, 36, 24)` | Failed; repeat not run |
| R5 Deferred Initial Creation | 18033 | First create Tray at `RunEvent::Ready`, then create and initialize the dependent widget | `(-1, 1268, 36, 24)` | Failed; repeat not run |

R1–R4 each started from a freshly measured Off-screen Bottom state before applying only that candidate. A diagnostic-only bounded file trigger allowed external pre-action AX measurement after Ready. Only AppHandle crossed into worker threads; Tray handles were acquired and dropped inside callbacks guarded by `MainThreadMarker`. Workers queued the second callback, with no blocking sleep on the main thread. R1's locked public API internally removes/recreates the native status item, so the same main-thread rule was applied to it as to R4. The trigger and external AX mechanism were never added to production.

R4 reused the existing Menu and its already-registered handler. The rebuild did not invoke `on_menu_event` again: locked Tauri registers those handlers globally, and rerunning that registration would duplicate actions. Registry absence was asserted after removal and presence after rebuilding. The final AX observation had one status-item child. Menu action delivery was not accepted or click-tested because placement failed.

R4 changed the associated native window's `window_visible` from false to true while leaving AX placement at the bottom. This setup-versus-Ready native behavior difference justified the conditional R5 diagnostic experiment; it was not itself evidence of recovery. R5 deferred both Tray creation and dependent widget creation, with existing widget initialization afterward. It remained a diagnostic-only startup variant.

All final native inspections retained `main`, image/menu/tooltip binding, hidden=false and alpha=1.0. R1 and R4 reported internal window visible=true; R2, R3 and R5 reported false. None of these internal-window values overrode the failed revealed-menu-bar AX result.

The hashes below and the baseline hash above identify the actual ad-hoc-signed executables launched, after bundle signing.

| Diagnostic binary | SHA-256 |
| --- | --- |
| R1 | `7bc4f4f24da340a4b79ba69e752dc08f9c9c9a377fb6854cf38af2206e7b1838` |
| R2 | `28dc788360b0ed746781759446cb9f893e53ab9f19cb220237b376241be044fb` |
| R3 | `4f8a9483bd65699eeeb42350ac51db53a4c1c7478d79feda85938dae0b2b2079` |
| R4 | `7a34089ab962cac7bb1a79c0318e62e221dcb79a20866b7c2ef3bd026ae6ed8d` |
| R5 | `4061328af50a970c7912e677e7dd87c26fdf093cf4fa916be0335ace3aeee830` |

### Scope, verification and acceptance

All six isolated debug App builds, including their frontend asset builds, passed. Raw per-process AX/native observations, diagnostic source variants and a SHA-256 manifest remain in local temporary evidence, outside Git. This report is the only tracked change. `git diff --check` passed. Full frontend/Rust regression suites, audits, updater check, fmt and clippy were not rerun: no candidate passed the prerequisite for a production change. Previous test counts are not presented as this run's results.

Winning candidate: **None**. Production workaround scope and macOS 26+ version gate: **not implemented**. No dependency upgrade, third-party patch, new private API, coordinate repositioning, AX production dependency or Accessibility permission request was introduced. ActivationPolicy, LSUIElement, Dock/Space policy, branding assets, `icon_as_template`, Windows behavior and all product features remain unchanged. Version remains **1.1.0**, production identifier **app.quotafloat.desktop**.

Light Tray, Dark Tray, Tray Menu and CM identity remain **Needs Human Check**; no repaired validation App is ready for those gates. Prior accepted Real Data, Quota Week and observed-only semantics remain carried-forward results, not new tests. Branding remains Frozen; Fast and Manual Reset remain Deferred. Recovery investigation stops here under the task's R1–R5 limit. Any upstream or architecture work requires a separate task.

## macOS Tray native placement diagnosis — 2026-09-12

**Root Cause: Confirmed — macOS NSStatusItem Off-screen Placement.** This confirms the status item's measured placement failure, not an upstream dependency regression or a specific integration trigger. **Ready for macOS Tray Minimal Fix** means a separate fix task may proceed from this evidence; no production fix or full native acceptance is claimed here.

### Source and isolation

- Start source / remote feature HEAD: `7ca42c4f49f59b5b9f82e124eb9f289925912410`, clean `feat/v1.2.0` checkout. Production fix `3917d52e5f435f9cadd0da5952d096ab37496b43` remains in its ancestry.
- The initially running Observed Turns validation binary was verified against its provenance: source `3917d52`, binary SHA-256 `aef42222f0db347ef897780b2300122735eedf3e3ae88024c651b37dc5d50892`, isolated identifier `app.quotafloat.v120validation.real`. Only this report differs between that source and the current feature HEAD. Its AX result was collected again in this task.
- That instance was quit before launching a new diagnostic-only debug App built from an exported copy of the current clean source. Only main-thread, read-only Tray instrumentation was added to the temporary source. The external configuration and bundle use `app.quotafloat.v120validation.placement`; production preferences, SQLite and configuration were not changed or copied. Diagnostic binary SHA-256: `1a53890fc3a8bac0616a9621cefb9f48eb0d1ddc3313f699105df555ee859972`.
- Lost commit `032c2ea` was not recovered or reconstructed. Historical observations were not substituted for current results.

### Environment and coordinate interpretation

| Item | Current observation |
| --- | --- |
| macOS / build | 26.6.2 / 25G83 |
| Architecture / Rosetta | arm64 / No (`sysctl.proc_translated=0`) |
| Displays | One built-in Retina display; primary; no external display |
| Logical screen / visible frame | Both `(0, 0, 2048, 1280)` |
| Reported backing scale / notch inset | 1.0 / safe-area top 0; no notch inset reported |
| Menu Bar AX region | `(0, 0, 2048, 30)` |
| NSStatusBar thickness | 22 points; this is not substituted for the 30-point AX menu-bar region |
| Menu Bar auto-hide | Enabled (`_HIHideMenuBar=1`); left unchanged |

AX coordinates below use the top-left screen origin. Native AppKit window coordinates use the bottom-left origin. `AXExtrasMenuBar` and its single child identify each target's actual status item; the ordinary application menu items were not mistaken for the Tray. Accessibility was trusted. No screenshots or other applications' UI content were collected.

Automatic menu-bar hiding was an essential confounder: both healthy controls initially reported AX `y=-59`. A brief pointer hover at the top edge moved their status items to `y=3`, inside the menu bar; the pointer was then restored. Thus the controls' initial negative coordinates were **not** classified as defects. Codex Monitor's frame stayed at the bottom during the same revealed-menu-bar measurement.

### Current Codex Monitor native evidence

| Check | Result |
| --- | --- |
| Tray ID / `app.tray_by_id` | `main` / Present |
| Native inspection thread | Main thread |
| NSStatusItem / registered status bar | Present / Present |
| Button | Present; hidden=false; alpha=1.0 |
| Button local frame | `(0, 0, 34, 22)` |
| Image / logical size | Bound / 18×18; existing non-template setting unchanged |
| Menu / tooltip | Bound / Bound |
| Tray event handler | Unchanged application builder registers the handler; no click delivery acceptance is claimed |
| Associated internal window | Not visible; native frame `(0, 0, 34, 0)`; not used as proof of placement |
| AX extras menu bar / sole status-item child | Both `(-1, 1268, 36, 24)` |
| Placement with top menu bar revealed | **Off-screen Bottom**: outside the top menu bar and partly beyond the 1280-point screen |

The original provenance-verified validation instance and the newly built diagnostic instance independently produced the same AX status-item frame. Normal controls occupied `y=3` while the diagnostic instance remained at `y=1268`. The nonzero AX status-item rectangle, revealed menu-bar comparison and current native binding evidence establish the off-screen placement classification without relying on internal NSWindow height.

### Native controls and one-variable isolation

All controls were temporary, independent applications with normal run loops and Quit menus. Pure AppKit used a `CM` text status item and Accessory policy. The Tauri control used a generated monochrome 18px test image, no branding assets, Provider, Token service or quota UI. Its initial version had no plugins or WebView. The control and product lockfiles agreed on all five dependency versions below.

“In Menu Bar” in this table is automated native/AX placement evidence while the menu bar is revealed. It is not a human Light/Dark or pixel-legibility acceptance. Human visibility questions had not received a response during these measurements.

| Control / added condition | Native / revealed AX result |
| --- | --- |
| Pure AppKit Accessory | Window visible; button hidden=false, alpha=1; AX `(995, 3, 43, 24)` — In Menu Bar |
| Minimal Tauri Accessory | Image/menu bound; button hidden=false, alpha=1; AX `(961, 3, 36, 24)` — In Menu Bar |
| Add `LSUIElement=true` only | AX `(961, 3, 36, 24)` — In Menu Bar |
| Accessory in setup vs before `App::run` | Both actual native policy=1; both AX `(962, 3, 36, 24)` — In Menu Bar |
| Add one blank hidden 80×80 WebView | AX `(961, 3, 36, 24)` — In Menu Bar |
| Apply product window configuration with `create/visible/focus/fullscreen=false` | AX `(962, 3, 36, 24)` — In Menu Bar |
| Then add window-state with product flags and initial-state skip | AX `(962, 3, 36, 24)` — In Menu Bar |
| Then add single-instance registration | AX `(962, 3, 36, 24)` — In Menu Bar |
| Then add autostart initialization, without enabling autostart | AX `(961, 3, 36, 24)` — In Menu Bar |
| Regular/Accessory A/B, same binary, LSUIElement and blank hidden WebView; no plugins enabled | Actual native policies 0/1; Regular AX `(961, 3, 36, 24)`, Accessory AX `(963, 3, 36, 24)` — both In Menu Bar |

The tested additions were cumulative and each measurement enabled only one additional condition. None produced the first failing transition. This does not rule out untested combinations, complete product setup ordering, menu construction, native widget initialization or persisted per-app system state. No specific single integration trigger has been confirmed. Accessory alone and the tested policy-call timings did not reproduce the failure. Pure AppKit and the locked Tauri stack can obtain normal menu-bar placement in this login session, so a general macOS/session inability to host status items is not established.

### Dependencies, scope and checks

| Dependency | Locked version |
| --- | --- |
| tauri | 2.11.5 |
| tauri-runtime | 2.11.3 |
| tao | 0.35.3 |
| wry | 0.55.1 |
| tray-icon | 0.24.1 |

Relevant upstream: **none confirmed**. The tests did not isolate a tray-icon regression or an Accessory-only failure, so the conditional upstream/patch stage was not entered. No dependency was upgraded or patched in production, and no upstream PR is presented as a fix for this result.

Swift/AppKit control build, temporary Tauri control builds and the isolated instrumented App build passed. The App build included its frontend asset build; full frontend/Rust test suites, CI, audits and clippy were not rerun. `git diff --check` passed. The tracked change for this task is this report only; temporary source and diagnostic artifacts remain outside tracked source.

Production Changes: **None**. Branding: **Frozen; unchanged**. Version remains **1.1.0** in all three version files; production identifier remains **app.quotafloat.desktop**. Per the user's current task, previously accepted Real Data, Quota Week and observed-only semantics remain Passed; Fast and Manual Reset remain Deferred. Those acceptance statements are user-supplied prior results, not new tests in this diagnosis. No main merge, tag, release, settings change or production fix was performed.

## State and provenance — historical

**Ready for Quota Week Human Re-validation** — the observed-Turn contract below supersedes earlier quota-week row eligibility and empty-state statements. The previous full macOS human acceptance was **NOT PASSED**; its findings are recorded below. Implementation and historical cross-platform CI are complete. Branding: **CM Material Human Validation — Passed**; **FROZEN**. The complete approved reference has been verified and CM runtime assets reconstructed. Previous C + Bars visual/container conclusions remain invalidated. Final native platform gates remain outstanding. This is feature development, not release preparation. Version files remain **1.1.0** until all feature/native prerequisites in the task are met. No downgrade support is claimed.

- Main baseline: `f99f084641c6d28d703bb20da002443bb1864ea8`.
- Locked icon source / feature start: `297476c9c818174e7a53585041db34fc5be87e9e`, with the exact baseline parent.
- Branch: `feat/v1.2.0`; independent clean checkout established before implementation.
- Phase 0: `5baf80e`; data: `303dd2b`; UI/native integration: `96f0841`; runtime branding: `f2ac610`; final period/precision corrections: `38ec22f`.
- Existing historical dirty checkout was not modified. No main merge, tag, release, or historical branch deletion was performed.

## Final quota-week observed-Turn semantics

**Ready for Quota Week Human Re-validation**. This section supersedes earlier statements that observation-less historical Turns should remain visible with a dash. The user's preceding Gate 1–3 feedback was broadly positive; it does not constitute acceptance of this new filtering behavior or the remaining native gates.

- Verified start: `7579da76e886e70df18269cff5aa756973d3e2fc`, clean `feat/v1.2.0`, equal to fetched origin.
- Fix / validation implementation source: `3917d52e5f435f9cadd0da5952d096ab37496b43`.
- Production scope: quota-week Turn eligibility, exclusive QueryAt boundary, and bilingual empty-state text only. Schema remains **3**.

### Phase 0 disposition

Manual-reset logical-cycle proposal: **Not implemented**. The current Reset Credit control only displays expiration information (`QuotaCard.tsx`); the Provider client reads usage and credit availability with GET requests (`codex.rs`). There is no reset execution path or success acknowledgement in the registered backend commands. Provider resetAt behavior following an external manual reset is unconfirmed.

**Manual Reset: Deferred — No Observable Success Signal.** No Schema 4, local cycle table, reset button, guessed success timestamp, percentage-rise heuristic, or extra polling was added. No real reset credit was consumed or requested for validation.

### Historical contract — Superseded by current-period historical Turn restoration

The following observed-only contract is retained as historical evidence and is no longer the product contract.

Quota week returned **the current Provider weekly period + observed Turns only**:

- A valid seven-day Provider window supplies `start = resetAt - 604800 seconds`; `start <= QueryAt < resetAt`. Invalid/unavailable windows remain unavailable, with no calendar-week fallback.
- Only completed Turns with `start <= completedAt < QueryAt` and `completedAt < resetAt` are eligible. Existing active Fact ownership and Token aggregation rules are unchanged.
- The existing first same-period observation association is unchanged: matching `weekly_reset_at`, `completedAt <= observedAt <= completedAt + 15 minutes`, and `observedAt <= QueryAt`.
- Missing observations exclude the entire Turn row. Normal returned rows have finite Weekly Remaining in `[0, 100]` and a non-null observation timestamp. Frontend defensive dash rendering remains available for malformed IPC data.
- Ascending completion order, internal scrolling, four-column headers and tab labels remain unchanged. No limit, pagination or history selector was added.
- Empty zh: **当前额度周暂无可用记录**. Empty en: **No observed turns in this quota week**.
- Automatic reset contract: a new Provider resetAt selects its own period and observations; old-period results cannot repopulate the view through a stale query. This is deterministic fixture evidence, not a claim of observing a real automatic reset.
- Historical `model_turns`, `model_identities`, `token_facts` and `quota_snapshots` remain persisted. Overview, all By Model periods, Token accounting, attribution, Effort and quota persistence are unchanged.

### Regression evidence

Test-first: four of five new backend observed-Turn tests failed against the old implementation; the Provider rollover test already passed. Both updated bilingual empty-state tests failed before the text change.

| Check | Result |
| --- | --- |
| npm ci / updater policy | Passed |
| Frontend | **177 passed / 14 files**, up from 174 |
| Rust | **94 passed / 2 existing ignored**, up from 89 passed |
| Observation eligibility | Passed: no observation, pre-observation historical/backfilled Turns, before-completion, after 15 minutes, cross-period, invalid remaining, valid zero/15-minute endpoints, and shared observation across parallel Turns |
| QueryAt half-open interval | Passed: completion before/equal/after QueryAt; observation exactly at QueryAt remains allowed |
| Provider rollover / restart | Passed: R1 returns only A, R2 only B, including after reopening and rescanning; history retained |
| Stale query | Passed: pending R1 observed rows rejected after R2; only R2 rows published |
| Accounting / history regression | Passed: exact Token values, Overview/By Model snapshot, accounting dump and snapshot history retained; existing ownership and reconciliation tests retain their assertions with valid observation fixtures |
| Frontend build | Passed |
| Full / production audits | **0 vulnerabilities** each |
| fmt / clippy all targets, locked, warnings denied | Passed |
| git diff --check | Passed |

### Real Data handoff

A new isolated macOS arm64 debug validation package is prepared from the fix source above. Its launcher uses the existing real, read-only Codex session input and normal Provider path, and explicitly clears both synthetic environment switches. It retains validation identifier `app.quotafloat.v120validation.real`, separate from production preferences, SQLite and window state. No production v1.1 database is copied or upgraded. Package paths and checksums are kept in local ignored artifacts.

Package build/signature/archive verification: **Passed**. Resolved bundle and embedded validation identity, version, frozen App icon, launcher machine-code/string sections, binary SHA-256, ZIP SHA-256 and archive integrity were checked. The new package has not been launched in this task; quit the previous Real Data validation instance before opening it. This is a local ad-hoc signed validation package, not a release.

| Human check | Result |
| --- | --- |
| Current-period observed rows only | **Needs Human Check** |
| Historical rows with missing Weekly Remaining removed | **Needs Human Check** |

Only recheck the quota-week list: rows must have actual Weekly Remaining observations, or show the new empty state. No request to perform a manual reset, and no claim of real-data human acceptance. Continue the remaining Gate 4+ separately after this semantic re-validation.

Branding **FROZEN**; Fast Mode **Deferred — Source Not Confirmed**. Version **1.1.0**, production identifier **app.quotafloat.desktop**. No main merge, tag, release or release preparation.

## Acceptance-fix validation — historical result before observed-Turn semantics

This historical section superseded the older native handoff matrices at that time. The final observed-Turn contract above now governs quota-week eligibility and empty-state text. It does not declare final macOS human acceptance.

- Start SHA: `0f1875ad4bbd5eafaeca70c773242409e1e389e5`, clean `feat/v1.2.0`, equal to fetched origin.
- Findings/feasibility commit: `9d7fbe1`.
- UI and native validation build source: `f9e2e5bc98b2429f851530d4a60e003b80597e94`.
- Scope: visible quota-week labels and CSS alignment, synthetic layout regression fixtures/test script, and documentation. Production real-data input, Provider, Token accounting, Quota Snapshot, schema, native window behavior and activation policy are unchanged.

### Real Data Native Validation

| Check | Result |
| --- | --- |
| Real Data Source | **Passed** |
| CODEX_HOME sessions / archived_sessions | Read-only real input; no embedded synthetic session; no JSONL writes |
| Synthetic quota / short-window environment | Both explicitly unset by the packaged local launcher |
| Real Provider | Used; normal successful Provider result observed in native UI |
| Preferences / SQLite | Isolated under validation identifier `app.quotafloat.v120validation.real`; no production v1.1 database copied or upgraded |
| Isolated database | Schema 3; read-only integrity check returned ok |
| Overview / real Token data | **Passed** — populated native view from real local logs |
| By Model | **Passed** — populated native model section with real model labels, no synthetic labels |
| Quota week | **Passed** — real completed Turn rows, effort, Token and weekly-observation cells visible |
| Missing historical weekly observation | Dash observed; valid unknown state, not a failure |

The macOS arm64 debug app was rebuilt from the UI commit with an external validation config, a package-only launcher and local ad-hoc signing. The launcher uses real local input and clears synthetic switches even when inherited. The build, bundle signature and ZIP integrity checks passed. It is a local machine-specific validation artifact, not a release or portable distribution. No real usage amounts, model histories, screenshots, account information, usernames or machine paths are included here or committed. Synthetic fixtures remain available separately for deterministic QA.

### Quota Week UI and regression

- Chinese tab: **额度周**. English tab: **Quota week**. Internal `turns` mode/API unchanged.
- ARIA: **额度周 · 当前额度周逐轮明细** / **Quota week · Turn details in current quota week**.
- Empty: **当前额度周暂无已完成对话** / **No completed turns in this quota week**.
- Unavailable: **当前额度周不可用** / **Current quota week unavailable**.
- Grid: `minmax(0, 1fr) 50px minmax(0, 70px) 48px`, gap **4px**. Effort header/value both right-aligned; Token and Weekly remain right-aligned. Model has slightly more available width than the previous grid and retains ellipsis.
- Actual browser layout matrix: **24 passed** (three skins × zh/en × light/dark × full/compact). Fixture includes High, xHigh, Max, Ultra, Minimal; short and long model slugs; short/万/亿/extreme Token values and partial asterisks; integer/high-precision/unknown Weekly values.
- Header/cell alignment, untruncated Effort labels, no Token horizontal overflow, non-overlapping columns, Model ellipsis and internal scrolling passed. Extreme synthetic integers retain the existing wrapping behavior; no new formatting or precision policy was introduced.
- Browser tests check actual outer scroll behavior, not decorative clipped scroll bounds; card dimensions remain 306×506 / 306×452. Native logical height acceptance remains separate.
- Reproducible script: `scripts/test-quota-week-layout.mjs` with a local Playwright module and Chromium executable supplied through its documented environment variables. Output is ignored local synthetic evidence, not committed screenshots.
- Test-first evidence: the updated label/ARIA tests failed **18** cases against the prior UI and passed after the change. The initial browser matrix detected Effort/header alignment failures; the final matrix passed. Sandbox Chrome launch failure was an environment restriction, resolved by the permitted isolated local test run.

### Fast Mode disposition

**Deferred — Source Not Confirmed.** A nested metadata follow-up found `turn_context.payload.collaboration_mode.mode = default`; it does not establish Fast/Normal semantics and is not used as a Fast source. No reliable Fast JSON path, Fast/Normal enum mapping, Turn binding or same-session switch evidence is confirmed. No inference, Fast storage field, lightning indicator or Schema 4 migration was added. Unknown is not classified as Normal.

### Automated regression

| Check | Result |
| --- | --- |
| npm ci | Passed; unchanged lockfile |
| Updater policy | Passed |
| Frontend | **174 passed / 14 files** |
| Frontend build | Passed |
| Full / production npm audits | **0 vulnerabilities** each |
| Rust | **89 passed / 2 existing ignored** |
| Schema migration | Existing regression suite passed; no new migration, Schema 3 retained |
| fmt | Passed |
| clippy all targets / locked / warnings denied | Passed |
| Synthetic browser layout | **24 passed** |
| Native debug app / signature / ZIP | Passed |
| git diff --check | Passed |
| Branding, Accessory policy, native Rust code, version/lockfiles | Unchanged from task start |

### Remaining macOS human re-validation

| Gate | Current evidence / next gate |
| --- | --- |
| Finder / App Bundle CM icon | Bundled icon matches frozen source; actual Finder observation Needs Human Check |
| Menu Bar Tray CM icon / menu | Needs Human Check |
| Overview / By Model / Quota week real data | Passed native basic check as above |
| Four-column layout / three skins | Synthetic browser checks passed; native human appearance review pending |
| Fast indicator | Deferred; no implementation |
| Internal scroll / click isolation | Automated composition/layout checks passed; native human check pending |
| Window Drag / Pure Hover | Needs Human Check; click expansion is not hover proof |
| Full / Compact native heights | Needs Human Check; do not force a synthetic Provider result and call it real-data validation |
| Same-process height transition | Needs Human Check; no live transition claimed |
| Dock icon residency | **N/A — intentional macOS Accessory application** |
| Full-screen Space / four-edge dock | Still required as subsequent final native gates |
| Windows real-machine gates | Still pending; no Windows human acceptance claimed |

**Final State: Ready for macOS Native Re-validation — Fast Mode Deferred.** Branding remains Human Passed / Frozen. Version **1.1.0**, production identifier **app.quotafloat.desktop**. No version bump, final versioned feature CI, main merge, tag, release or release preparation.

## Native acceptance findings and Fast Mode feasibility — 2026-09-12

**macOS Native Acceptance: NOT PASSED.** Human findings supersede the previous handoff state; this does not mean production data is unavailable.

| Finding | Classification / disposition |
| --- | --- |
| Synthetic fixture still active during validation | Validation issue; prior test package intentionally embedded synthetic sessions and quota |
| Real local Codex data not yet validated | Validation issue; repeat with read-only real session input, normal Provider fetch and isolated app preferences/SQLite |
| Turn column spacing needs refinement | Product UX issue; align Effort right and rebalance four columns |
| Fast mode representation is missing | Metadata feasibility gate; do not infer from model, effort, speed or Token |
| Turn-mode label needs semantic correction | Product UX issue; rename the visible tab to 额度周 / Quota week |
| Dock icon | **N/A — intentional macOS Accessory application**; not an icon loading failure |

`src-tauri/Info.plist` retains `LSUIElement = true`; `src-tauri/src/macos_widget.rs` retains `ActivationPolicy::Accessory`. App icon acceptance checks Finder/App Bundle and the Menu Bar Tray. Dock residency is not required. Full-screen Space and four-edge dock acceptance remain separate outstanding gates.

### Fast Mode Phase 0

- **Fast Mode Source: Not Confirmed**.
- Read-only sample: 80 most recently modified local session/archive JSONL files, 483 `turn_context` metadata records. Only metadata keys and candidate scalar enums were inspected/output; no prompts, chat, code, reasoning/tool bodies, auth, identities or usage amounts are evidence in this report.
- No Fast/service-tier candidate scalar field was found in the sampled `turn_context.payload` metadata. This bounded sample does not prove that no other log format contains such a field.
- Exact JSON path: **none confirmed**. Fast/Normal values: **none confirmed**. Turn-level binding and same-session Fast/Normal switching: **not confirmed**.
- **Fast Mode = Deferred — Source Not Confirmed**. No production Fast implementation, lightning indicator, inferred Normal label, metadata rescan or Schema 4 migration is authorized by these findings. Schema remains **3**; unknown remains unknown.
- Production real-data input/Provider implementation remains unchanged. Validation packaging must disable both synthetic environment switches and use isolated app data with read-only real Codex session input.
- Branding remains **Human Passed / FROZEN**. Version remains **1.1.0**, production identifier **app.quotafloat.desktop**.

## Final native acceptance provenance — 2026-09-12

- Start SHA: `9142ab2b4a440726581628d014a0f90ceb8ba804`; clean feature worktree, matching fetched remote.
- Branding Acceptance Docs SHA / `NATIVE_ACCEPTANCE_SOURCE_SHA`: `88e598a159a92a4fd603501d4005cf95fe380259`; pushed and verified equal to fetched `origin/feat/v1.2.0`.
- Native testing must use this source or an explicitly verified equivalent production tree. This subsequent report-only change does not alter production code or assets.
- No current-source validation binary was built or launched in this task. Historical binaries and the earlier native observations below are not final acceptance for this source.
- Current CUA exposes click, drag, scroll and keyboard actions, but no pure pointer-move API. It cannot prove the mandatory pure hover cycles. No Windows real-machine control endpoint is available.
- Per the task's human-handoff stop rule, current native gates are handed off without substituting browser tests, raster review, geometry tests or historical CI. No native product failure was observed in this task.
- Production changes: **None**. Version remains **1.1.0**; production identifier remains **app.quotafloat.desktop**. No full CI rerun, version bump, merge, tag, release or release preparation.

## macOS Final Native Acceptance

Current-source native session: **pending human operation**. The historical native table later in this report is not this final gate matrix.

| Gate | Result |
| --- | --- |
| App Icon | Needs Human Check |
| Light Menu Bar | Needs Human Check |
| Dark Menu Bar | Needs Human Check |
| Tray Menu | Needs Human Check |
| Pure Hover (5 consecutive cycles) | Needs Human Check |
| Overview | Needs Human Check |
| By Model (five periods) | Needs Human Check |
| By Turn | Needs Human Check |
| Internal Scroll | Needs Human Check |
| Token Click Isolation | Needs Human Check |
| Window Drag | Needs Human Check |
| Left Dock | Needs Human Check |
| Right Dock | Needs Human Check |
| Top Dock | Needs Human Check |
| Bottom Dock | Needs Human Check |
| Full-screen Space | Needs Human Check |
| Full Height 314×514 | Needs Human Check |
| Compact Height 314×460 | Needs Human Check |
| Runtime compact → full | Needs Human Check |
| Runtime full → compact | Needs Human Check |
| Default | Needs Human Check |
| Blur | Needs Human Check |
| Computer | Needs Human Check |
| Quit / Restart | Needs Human Check |

## Windows Final Native Acceptance

Windows tested Source SHA: **pending**. Required source: `88e598a159a92a4fd603501d4005cf95fe380259`. No Windows human gate is accepted without a real-machine result.

| Gate | Result |
| --- | --- |
| App Icon | Needs Human Check |
| Tray | Needs Human Check |
| Tray Menu | Needs Human Check |
| Pure Hover (5 consecutive cycles) | Needs Human Check |
| Overview | Needs Human Check |
| By Model | Needs Human Check |
| By Turn | Needs Human Check |
| Click Isolation | Needs Human Check |
| Window Drag | Needs Human Check |
| Full Height | Needs Human Check |
| Compact Height | Needs Human Check |
| Runtime Height Transition | Needs Human Check |
| Internal Scroll | Needs Human Check |
| Default | Needs Human Check |
| Blur | Needs Human Check |
| Computer | Needs Human Check |
| Quit / Restart | Needs Human Check |

## Native human handoff checklist

### Source and data isolation prerequisites

On the real Windows machine, start with a clean repository and run `git fetch origin`, `git switch feat/v1.2.0`, `git pull --ff-only`, then `git rev-parse HEAD`. Compare against `NATIVE_ACCEPTANCE_SOURCE_SHA`. If the branch includes this later report commit, verify production equivalence with `git diff --name-only 88e598a159a92a4fd603501d4005cf95fe380259 HEAD`: only documentation may differ. Stop on unexpected differences. Do not use an old candidate, old installer or the v1.1 release executable.

For both platforms, build an isolated **debug-only** validation app from that verified source using an external, uncommitted Tauri config with validation identifier `app.quotafloat.v120validation`; retain committed production identifier `app.quotafloat.desktop`. Confirm the actual validation identifier and resolved preferences/database directories before launch. Use only the existing synthetic session fixture as the Token input; do not point the fixture at the real user session directory or copy a real database. Enable existing `CODEX_MONITOR_SYNTHETIC_QUOTA=1`; begin with `CODEX_MONITOR_SYNTHETIC_SHORT_WINDOW=0`. Fixture setup and current-source build are still pending on each platform; these instructions are not evidence that a package is ready or has run. Keep machine-specific paths and synthetic input outside committed documentation. Do not publish native screenshots or personal data.

### macOS operations

1. Inspect the App Bundle/Finder icon and Dock/App Switcher where visible; switch macOS Light then Dark and inspect the real menu-bar CM icon for identity, clipping and stale artwork.
2. Open the real Tray menu. Test Show/Hide, Refresh, Settings as present, Theme submenus and release-download entry. Do not enable login startup as part of fixture testing.
3. With pinning disabled, move the real pointer in/out for **five consecutive cycles**, without clicks or keyboard expansion; check flicker, sticking and position stability.
4. Click Overview, By Model (all five periods), By Turn, rows and exact Token values; scroll both populated lists. Confirm immediate response, no accidental drag and no outer scrollbar. A missing By Turn observation shown as a dash is valid.
5. Drag from the allowed region to left/right/top/bottom and one bottom corner. At each edge perform collapse → expand → collapse; check anchor, inset and screen bounds.
6. Enter a normal app's native fullscreen Space, check overlay/hover, exit and check position.
7. In the **same process**, toggle Tray `Test: simulate 5-hour quota` on/off. Confirm actual native logical size **314×460 → 314×514 → 314×460**, correct block visibility, no blank residue/clipping/jump; repeat while bottom-docked. Separate launches do not prove transition.
8. Switch Default/Blur/Computer, inspect Token layout and absence of old decorations; Quit through Tray, restart the same isolated app and check preferences and position.

### Windows operations

1. Confirm source and isolation above; inspect Explorer/app/taskbar icons where visible and the real system Tray for the current CM identity.
2. Exercise real Tray Show/Hide, Refresh, Theme and existing other items; perform five pure pointer hover cycles.
3. Test Overview/By Model/By Turn, period buttons, rows and Token values: immediate UI response, no drag or multi-second delay. Test permitted window dragging and internal-only scrolling.
4. Check full/compact heights and same-process toggle where available; inspect Default/Blur/Computer; Quit from Tray and restart, checking Widget/Tray/preferences.

Return platform, tested source SHA and per-item Passed/Failed/Needs Human Check (or explicit all-Passed confirmation after completing every listed gate). For a failure include Gate, Steps, Observed behavior and Expected behavior; stop that platform on P0/P1 and do not fix in this task. Unreadable macOS Tray requires **Blocked — macOS Tray Adaptation Required**; other P0/P1 native failures require **Blocked — Native Acceptance Failure**.

**Prior handoff state (superseded): Ready for macOS Native Human Validation.** Windows preparation is documented; real-machine testing remains pending. Only after both platforms explicitly pass may the state become **Ready for v1.2.0 Version Bump & Final Feature CI**; this task still stops before any version bump.

## Feasibility and data contract

All four Phase 0 gates are Confirmed; see [feasibility](v1.2.0-turn-quota-feasibility.md). Effort is exactly `turn_context.payload.effort`, stored without inference; display normalization only. Model comes from `turn_context.payload.model`. Response records explicitly bind thread/turn/response identities. `event_msg.payload.type = task_complete` uses `payload.completed_at` in Unix seconds. Start, successful completion and abort are distinct parsed events; aborts and contradictory completion timestamps do not produce precise rows.

Schema 2 to 3 adds effort/conflict/completion columns, `quota_snapshots` and metadata indexes. All DDL, metadata-checkpoint reset and `user_version` mutation are transactional. Early, intermediate and final-DDL injected failures restore the original schema, metadata checkpoints and accounting facts; restart is tested. Accounting parser version remains 1. Only supplementary metadata is rescanned; accounting cursors, active/reconciled fact rules, definitions, deduplication and normalization semantics remain unchanged. Regression tests compare Overview and By Model amounts before/after migration and rescan. Legacy attribution requires the existing exact fact/source ownership evidence. No independent usage ledger is introduced.

The third Token mode uses one reliably completed turn per row, sorted oldest first in the current real weekly quota period. It does not group sessions/tasks or collapse repeated model/effort combinations. Tokens aggregate existing eligible active facts and cross IPC as decimal strings, formatted with BigInt. Model conflicts are explicitly unidentified; effort conflicts/missing fields show a dash. Unproven fact ownership is excluded. Future facts retain existing time deferral.

## Quota observations

Only an existing fresh successful Codex provider refresh can persist a valid weekly observation. Cache reads do not create observations. No additional HTTP requests, completion-triggered requests or polling were added. Debug 5h display overrides run after persistence.

- Valid weekly period: canonical `resetsAt`, duration exactly 604800 seconds, start at reset minus 604800 seconds; no natural-week fallback.
- Valid finite original percentage: inclusive range 0–100, preserved as SQLite REAL without UI rounding in storage; invalid percentages explicitly reject.
- Observation records actual refresh observation time and resolved source root. Missing/unresolved root skips persistence rather than guessing.
- Association: same reset period, first observation at/after completion, inclusive maximum 15 minutes, never later than query time. No earlier/cross-period/interpolated observation.
- Parallel turns may share one observation. Percentages may rise, including provider/reset-credit behavior; no monotonicity constraint.
- No eligible history means null / dash. Current quota is never used to infer old percentages.

## UI and native geometry

Three skins and two languages retain the established design. Expanded Default/Computer decorations are removed; Orb, Computer background/stripes and error assets remain. Absent shortWindow with available weekly quota removes the whole 5h block. Provider error states and Reset Credit remain.

All user-visible Incomplete text, including tooltip and aria additions, is removed; internal partial state and numeric `*` remain. By Turn has separate model, effort, exact Token and weekly observation columns, full-model hover, right-aligned numeric columns and internal list scrolling. No second period selector is added. Quota-boundary changes immediately clear old turn rows, including delayed or failed queries. High-precision percentages use an ellipsis in the narrow column while preserving full precision in the focus/hover tooltip and accessible label; both tooltips were visually checked above the internal scroller.

A 36-case real Chromium matrix measured visible dimensions **306×506** (full) and **306×452** (compact). With the existing 4px safe inset, native logical dimensions are **314×514** and **314×460**. CSS and Rust use these deterministic modes; serialized expand/collapse/appearance transitions prevent races. Height growth updates native geometry and clamps in the monitor work area. Tests cover both modes at multiple scales, bottom/right anchoring, negative-origin monitors and small bounds. No content-driven ResizeObserver is used. A concurrent height/theme sync during the first expanded drag preserves the current position instead of returning to the old collapse anchor; a dedicated regression test covers both heights.

Token content moves upward (full 284px, compact 230px); By Model list viewport measures 115–117px versus the previous 88px, and By Turn 117–119px. Sparse lists can still leave unused list space because heights are deterministic. Browser checks found and fixed outer focus scrolling caused by decorative overflow; only lists scroll. This matrix is layout evidence, not human acceptance.

## CM Branding Human Acceptance

**CM Material Human Validation — Passed**

Recorded 2026-09-12 from the user's explicit human review confirmation for baseline `9142ab2b4a440726581628d014a0f90ceb8ba804`.

Human reviewer confirmed:

- Approved CM identity retained: **C + M + segmented monitoring arc**
- Tile material accepted
- C material / glow accepted
- M material accepted
- Segmented arc accepted
- 256px accepted
- 512px accepted
- 1024px accepted
- Current material differences from the original raster reference are acceptable technical reconstruction differences
- No further visual polish is required

**Branding Status: FROZEN**

- Branding source of truth: `docs/branding/v1.2/app-icon-reference.png`
- Approved reference SHA-256: `373af7dca75b1e45310c6c8b43602d44f16629c1f5225279f0efdb8c98af45c5` (rechecked at task start)
- Runtime technical source: `docs/branding/v1.2/app-icon-source.svg`
- Tiny source: `docs/branding/v1.2/app-icon-small-source.svg`

All source assets, generated visual parameters and Tray identity are frozen. A platform defect discovered during acceptance must be recorded and reported; any fix requires a separate minimal task. This confirmation does not establish any macOS or Windows native gate as Passed.

## Branding Correction — 2026-09-12

**CM Material Human Validation — Passed**

- Branding implementation commit: `cc230e8aa6341c10acdc34d453a918804dbced40`.
- Reconstruction start: `b2c4d41fd35f59a4381b1370f507aa5a999b3cb7`, clean `feat/v1.2.0`, matching origin after fetch.
- Approved source: `docs/branding/v1.2/app-icon-reference.png`, **1,316,884 bytes**, **1254 × 1254**, fully decoded.
- SHA-256: `373af7dca75b1e45310c6c8b43602d44f16629c1f5225279f0efdb8c98af45c5`.
- Identity: **C + M + segmented monitoring arc** on a dark rounded app tile.
- Editable main SVG reconstructs the tile, C/M geometry, gradients, six ticks and fading tail. The exterior presentation canvas and shadow are excluded. Material fidelity is a technical reconstruction accepted by the human reviewer.
- Tiny CM SVG serves 16/20px: three spaced ticks, reduced glow and stronger M edge. 24px and larger retain the main source. The newly reconstructed tiny SVG does not reuse the former C + Bars content.
- Tauri-generated Windows Square/StoreLogo, Android and iOS outputs replace the fallback set. All App/Tray/ICO/ICNS assets now derive from CM SVGs. Runtime Tray mapping remains `icons/tray-32.png`; behavior is unchanged.
- Old C + ascending bars, three-bar Tray and violet artifacts: **Superseded / Invalidated / Not active**. No old raster/contact-sheet is used as current evidence.
- [Branding source and mapping documentation](branding/v1.2/README.md); [technical validation](branding/v1.2/validation/technical-validation.json).
- [All sizes light](branding/v1.2/validation/cm-all-sizes-light.png), [dark](branding/v1.2/validation/cm-all-sizes-dark.png); [tiny light](branding/v1.2/validation/cm-tiny-light.png), [dark](branding/v1.2/validation/cm-tiny-dark.png).
- **Material visual acceptance: Passed. Native final acceptance: outstanding.** The earlier truncated-source blocker is resolved by the verified complete reference at the reconstruction baseline.
- Historical Phase 0, Schema 3, Token accounting, Quota Snapshot, By Turn, UI, geometry and CI evidence remains valid within its original scope. Only branding conclusions are replaced.

### CM Material Fidelity Polish — current validation

- Material commit: `c18cbe72ac6bbc916bb30495a9a4febbf4c0c792`.

- Start: `93ce0b2684cb7272bb30ccc8cb56fa045d0c3bb8`, clean `feat/v1.2.0`, equal to origin after fetch.
- Reference unchanged: **1,316,884 bytes**, **1254 × 1254**, SHA-256 `373af7dca75b1e45310c6c8b43602d44f16629c1f5225279f0efdb8c98af45c5`.
- User-confirmed baseline: CM identity, C/M/arc, 16/20/24/32px, Tray direction and large-size geometry. Large-size material fidelity has now been accepted by the human reviewer; native platform gates remain separate.
- **Geometry unchanged:** 19 original geometry records match, including C/M/segments/tail, Tile coordinates/radius, clip geometry, viewBox and stroke widths. Three material overlays only reuse the existing C/M path definitions without transforms or new contours.
- **Tiny source unchanged:** source and 16/20px App/Tray plus iOS 20px rasters are byte-identical. Generator and existing validator are unchanged.
- **Tile polish:** multi-stop surface shading, restrained vignette, softer rim, inner edge light/shadow.
- **C polish:** refined mint/cyan/blue stops, soft coloured glow, light/shade edge depth and subdued cyan/mint bloom.
- **M polish:** ice-white/light-blue material, softened left blue reflection, cold edge highlights and subtle upper-right lighting.
- **Segments / tail:** naturally attenuating green/cyan/blue-gray segment shading and subdued dark tail; geometry/count unchanged.
- **Small sizes:** 16/20 exact bytes preserved; 24/32 technical checks and enlarged tool-side review found no obvious clarity regression. No separate Tray adaptation was needed.
- **Alpha / clipping:** all generated PNGs pass the existing fringe/alpha checks; no new transparent interior holes, unchanged raster bounds, glow support fits inside its filter region and Tile. Some 24–256px antialias fringe alpha values change with the filters; 512/1024 alpha channels are byte-identical. Geometry attributes are unchanged.
- **Determinism:** repeated generator and material review produced 77 byte-identical files.
- **Containers/platforms:** 63 PNGs decode; ICO 8 entries and ICNS 11 entries decode and match their generated sources; Windows, Android and iOS assets regenerate successfully.
- **Current regressions:** npm ci, updater policy, frontend **174 passed / 14 files**, build, both audits (**0 vulnerabilities**), Rust **89 passed + 2 existing ignored**, fmt, clippy `-D warnings`, and diff checks passed. No test or threshold was changed.
- **Frozen functionality/version:** no component, accounting, Schema, Quota, geometry/window behavior or version-file changes. Version **1.1.0**, identifier **app.quotafloat.desktop**.
- [Reference / Before / After — light](branding/v1.2/validation/cm-material-compare-light.png), [dark](branding/v1.2/validation/cm-material-compare-dark.png): actual 256/512/1024px rows; Before from the exact baseline. Reference is display-cropped/masked only; the approved source is untouched.
- [Reproducible geometry/tiny/material evidence](branding/v1.2/validation/cm-material-technical-validation.json).
- **Human material gate: Passed** by the explicit user confirmation in the Branding Freeze + Native Final Acceptance task. Tool inspection and technical checks are not its basis. Native final acceptance remains separate; no version bump, merge, tag or release was performed.

### Prior CM reconstruction technical verification

| Check | Result |
| --- | --- |
| npm ci | Passed; isolated cache, unchanged lockfile |
| CM generation | Passed; 63 platform/app/tray PNGs decode; repeat generation is byte-identical |
| SVG / alpha / crop | Main/tiny parse; transparent corners; no presentation canvas; dark alpha fringe; component colour probes passed |
| App sizes | 16/20/24/32/48/64/128/256/512/1024 generated; technical checks passed |
| Tray sizes | 16/20/24/32 generated with CM identity; technical checks passed |
| ICO | 8 entries: 16/20/24/32/48/64/128/256; each decoded and matched to its source raster |
| ICNS | 11 logical/Retina entries; each decoded and matched to its source raster; mappings in branding README |
| Windows / Android / iOS | All existing outputs regenerated and decoded; iOS opaque |
| Contact sheets | Four CM sheets; actual pixels and nearest-neighbor tiny enlargements reviewed technically |
| npm run check:updater-policy | Passed |
| npm test | 174 passed, 14 files |
| npm run build | Passed |
| npm audit --audit-level=high | 0 vulnerabilities |
| npm audit --omit=dev --audit-level=high | 0 vulnerabilities |
| cargo fmt --all -- --check | Passed |
| cargo test --locked | 89 passed, 2 existing ignored |
| cargo clippy --all-targets --locked -- -D warnings | Passed |
| git diff --check | Passed |
| Frozen production behavior and versions | Unchanged; diff restricted to branding assets, generation/validation scripts and branding documentation |

Technical checks and tool-side image inspection do not establish exact reference similarity or human CM/tiny readability acceptance. No native app was launched for this reconstruction. This is not a versioned final feature CI run or release qualification.

## Verification evidence

| Check | Result |
| --- | --- |
| npm ci | Passed using disposable cache; 0 vulnerabilities |
| Frontend | 174 passed across 14 test files |
| Rust/macOS | 89 passed, 2 existing ignored; includes 13 new backend cases and 3 geometry cases |
| Migration | v2→v3 success, early/mid/late rollback, restart, accounting and metadata preservation passed |
| Quota / effort | Precision, invalid values, timing/period boundaries, parallel observations, rising percentages, repeated turns, conflicts, legacy/reconciliation and clock rollback passed |
| Browser UI | 36 combinations plus long model/extreme Token/null/partial/empty/error tests; no outer scrolling |
| Production web build | Passed |
| Native debug app bundle | Built and launched successfully with isolated validation identifier and synthetic Codex data |
| macOS native compact | 314×460 logical-window screenshot; Default Chinese Overview/By Model/By Turn, five-period button interaction and internal scrolling observed; Blur Chinese Overview/By Turn also observed |
| macOS native full | 314×514 logical-window screenshot; Computer English Overview/By Turn observed |
| Native drag | Non-Token drag gesture exercised without breaking the view; edge docking and fullscreen transitions not independently verified |
| App/tray startup | Historical integration startup only; all previous native Tray icon conclusions are Superseded / Invalidated. CM appearance and tray-menu interaction are not verified |
| Windows | Final CI: 85 passed, 2 ignored; clippy and Windows bundles passed. Earlier readiness timeout did not recur; its unchanged 5-second gate and safe diagnostics remain. Real system Tray/click/drag not verified locally |
| Both npm audits | 0 vulnerabilities |
| Updater policy | Passed; unchanged |
| cargo fmt / clippy -D warnings | Passed |
| git diff --check | Passed |

Native fixtures are debug-only, require an isolated identifier and use explicit synthetic environment switches. They are unavailable in release builds. Formal `app.quotafloat.desktop` identity remains unchanged. All native evidence above uses synthetic values and a separate test database; no v1.1 database was upgraded for UI testing.

## Remote CI follow-up

Initial CI run `34609225318` on `b98e2d0` passed frontend; Windows failed the existing startup readiness deadline in `concurrent_refresh_is_coalesced_and_queries_use_committed_generation` before its concurrent stress phase. All new turn/quota/migration tests passed. This does not establish a production defect or justify relaxing the deadline. The test now includes safe status/generation/warning-code diagnostics; the five-second assertion remains unchanged. The intermediate superseded run `34609881782` was cancelled so the final revision can be tested. Final [CI run 34610695055](https://github.com/bennick1/codex-monitor/actions/runs/34610695055) on **`6d5251a4340a1c5ffca9a071e8167ca34ae9fd27`** completed successfully: frontend 174 passed; macOS 89 passed + 2 ignored; Windows 85 passed + 2 ignored; both strict clippy checks and native bundles passed. The readiness timeout did not recur with the original five-second gate; its root cause has not been proven, and no production backoff or timeout was changed. The final local isolated debug package was rebuilt from this code revision and its Blur startup/expand/By Turn display rechecked. Subsequent delivery commits change only this report; they are not a separate binary build source. CI artifacts are feature-validation outputs, not a release or release candidate.

## Privacy

Phase 0 emitted only metadata keys, effort enums and structural checks. Public documents and fixtures contain no real prompts, conversations, code bodies, reasoning/tool content, auth/JWT/access tokens, token amounts, quota percentages, model usage histories, raw session/turn identifiers, usernames or machine paths. Persisted production fields are limited to supplementary identities/metadata, completion timestamps, token accounting facts and quota observations. No real screenshot is committed.

## Known limitations and remaining gates

- Weekly quota is an account Provider Snapshot, not one turn's independent consumption; parallel turns may share it and provider timing may lag.
- Pre-v1.2 quota history does not exist; old rows may show a dash. The 15-minute maximum can also leave recent rows without observations.
- Observations arriving before source-root resolution are skipped. Failed DB persistence does not add an HTTP retry; a later normal refresh may provide another eligible observation.
- Token amounts remain local Codex log statistics; there is no cross-device merge or official Token-to-Quota conversion.
- Metadata sampling does not prove all historical log formats; absent/unreliable fields remain unknown.
- Previous small-size branding review is Superseded / Invalidated. CM runtime reconstruction, technical validation and current CM material human acceptance are complete; actual native Tray selection/legibility remains outstanding.
- Actual macOS menu-bar light/dark readability, Tray menu, fullscreen Space, dock/edge clamp, pure-hover cycles and Windows real-machine Tray/click/drag remain outstanding.
- Keep version at 1.1.0 pending the required gates; then bump only the specified version files in a separate commit.

## 2026-09-12 icon technical polish and native closure attempt

### Git provenance

- Verified clean start and fetched remote feature baseline: `3957d559332d96014edd7cfaec478068fa145d26`.
- Icon production fix: `35f3ed1` (`fix: refine small-size v1.2 icons`).
- Version bump: not performed; prerequisites remain incomplete.
- This report update is separate from the icon production fix. The feature history is preserved without squash.

### Icon Final Acceptance — Superseded / Invalidated

The entire former icon acceptance table is invalidated because it evaluated the wrong C + Bars candidate: App 16/20/24/32px, detached violet segment correction, Tray 16/20/24/32px, tiny derivative mappings, contact sheets and ICO / ICNS container/source mappings. None is a current CM validation result. Independent human visual acceptance was never established.

### macOS Native

Superseded C + Bars artwork was built into an isolated debug App with synthetic input and an isolated validation identifier. The production identifier and production database were not changed. Initial sandboxed launch aborted; the explicitly isolated GUI launch outside the sandbox succeeded. A tool-launched instance without the fixture environment remained loading and was closed; it is not used as acceptance evidence.

| Gate | Result / necessary technical conclusion |
| --- | --- |
| Light Menu Bar | Not verified — native status-bar access timed out |
| Dark Menu Bar | Not verified — no valid native observation |
| Tray Menu | Not verified — status menu was not exposed by the available native interface |
| Pure Hover | Not verified — interface has no pure pointer-move action; click expansion is not hover proof |
| Dock edges: left / right / top / bottom | Not verified |
| Full-screen Space | Not verified |
| Full height | Passed — separate shortWindow-present fixture screenshot, 314×514 logical window |
| Compact height | Passed — separate shortWindow-absent fixture screenshot, 314×460 logical window |
| Runtime transition between heights | Not verified — separate fixture launches do not prove live switching |
| Overview / By Turn | Passed — current native WebView observed with synthetic data; model/effort/Token/quota columns present |
| Quit / restart | Passed — isolated fixture exited and restarted with the other height; not a Tray-menu Quit test |

The compact and full screenshots show only the established safe inset, without a large extra transparent area. The isolated instance was closed after validation, and its temporary keep-expanded setting was restored. Native screenshots are not committed. No color-readability failure was established, so **macOS Tray Adaptation Review Required** is not asserted.

### Windows Native

No real Windows control endpoint was available. The user explicitly instructed that Windows gates remain unverified. CI is not substituted for these tests.

| Gate | Result |
| --- | --- |
| Tray | Not verified |
| Tray Menu | Not verified |
| Hover | Not verified |
| Overview / By Model / By Turn | Not verified |
| By Turn model / effort / Token / weekly quota | Not verified |
| Token click isolation | Not verified |
| Original drag region | Not verified |
| Full / compact heights and transition | Not verified |
| Default / Blur / Computer skins | Not verified |
| Quit / restart | Not verified |

### Version

Package, package lock, Cargo manifest/lock and Tauri remain **1.1.0**. Identifier remains **app.quotafloat.desktop**. Version 1.2.0 is not authorized by the current gate results.

### Historical local verification — before CM correction

| Check | Result |
| --- | --- |
| npm ci | Passed |
| npm run check:updater-policy | Passed |
| npm test | 174 passed, 14 files |
| npm run build | Passed |
| npm audit --audit-level=high | 0 vulnerabilities |
| npm audit --omit=dev --audit-level=high | 0 vulnerabilities |
| cargo fmt --all -- --check | Passed |
| cargo test --locked | 89 passed, 2 ignored |
| cargo clippy --all-targets --locked -- -D warnings | Passed |
| Native debug App bundle | Passed |
| git diff --check | Passed |

### Final Feature CI

`FINAL_V1_2_FEATURE_CI_RUN_ID`: **not assigned**. A versioned 1.2.0 final feature build is not produced because native prerequisites remain incomplete. The historical run `34610695055` remains historical evidence only. Any push-triggered CI for this unversioned icon fix is an intermediate check and must not be represented as the final versioned feature build. Its run/head can be resolved from the feature branch CI history and delivery response.

**Prior handoff state (superseded): Ready for macOS Native Human Validation.** CM material human acceptance is Passed and Branding is FROZEN. Final native human gates remain outstanding; no product failure is asserted. Preserve the feature branch and version 1.1.0. Do not merge main, tag or publish.
